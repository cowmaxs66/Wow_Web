# 当前用户安装器说明

本目录保存发布包安装和卸载脚本。

## 职责
- 将发布包复制到当前用户目录 `%LOCALAPPDATA%\WoWFramework`。
- 创建桌面快捷方式和开始菜单快捷方式。
- 卸载时删除程序文件和快捷方式。

## 边界
- 不需要管理员权限。
- 不注册系统服务。
- 不复制或注册大漠 `dm.dll`。
- 卸载时保留 `data` 和 `logs` 目录，避免删除运行记录。
