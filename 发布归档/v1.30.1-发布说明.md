# v1.30.1 发布说明

`v1.30.1` 是桌面控制台直启热修复版本，重点解决用户直接双击 `WoW-Desktop.exe` 时出现 `127.0.0.1 拒绝连接` 的问题。

## 已包含
- `WoW-Desktop.exe` 启动时先检查本机 Server 是否可连接。
- 如果本机 Server 未响应，会自动启动同包内 `bin/management-server-core.exe --no-open-browser`。
- 自动启动 Server 后最多等待 8 秒，连接成功后再加载控制台页面。
- 如果 Server core 缺失或启动失败，显示项目自己的中文错误页，不再直接展示 WebView2 默认连接失败页。
- Server 自动启动日志写入 `logs/management-server.log` 和 `logs/management-server-error.log`。

## 验证结果
- `cargo fmt --all --check`：通过。
- `cargo test -p desktop-console`：通过，9 项测试全部通过。
- `cargo test --workspace`：通过，Client 55 项、desktop-console 9 项、Server 53 项、shared-types 11 项、launcher 3 项测试全部通过。
- `npm run build`：通过，Web Admin 版本为 `1.30.1`。
- `tools/package-release.ps1`：通过，三类 Windows zip 已生成。
- 直启 smoke：在不提前启动 Server 的情况下，从 Server 分包启动 `WoW-Desktop.exe --url http://127.0.0.1:18134`，自动拉起同包内 Server core，`/health` 返回 HTTP 200。

## 本地编译包
| 包 | SHA-256 | 大小 |
|----|---------|------|
| `WoW_Framework_v1.30.1_windows.zip` | `ceaa6d95f968c4928e4d849103584d98f6fc7a19ebada1a3cfbebfd1a339e4f8` | `8250446` bytes |
| `WoW_Server_v1.30.1_windows.zip` | `16ded0bc0527faf9b3f0545d5521cb38d8a9313734c025b1277fa82ddacf2cb0` | `1874653` bytes |
| `WoW_Client_v1.30.1_windows.zip` | `d8f39ad3a9fdb91ba39dfd1b2cd2d5808d1e63f585fc92d5d42cd8f6507e40d8` | `6127769` bytes |

## 本地路径
```text
target/release-package/WoW_Framework_v1.30.1_windows.zip
target/release-package/WoW_Server_v1.30.1_windows.zip
target/release-package/WoW_Client_v1.30.1_windows.zip
```

## GitHub Release 状态
当前环境未提供 `gh` CLI，也未暴露可创建 GitHub Release 和上传资产的工具，因此本次先完成本地三类包、源码归档和后续 tag 推送准备。远端 Release 待可用上传工具后补发。

- Git tag：`v1.30.1`
- 指向提交：`eb6d72e5a029e27f651d00b73f63de9843bf16fd`

## 安全边界
- 总包和 Client 分包继续包含 `DmBridge.dll`、`dm.dll` 和 `RegDll.dll`。
- Server 分包不包含 DM 运行文件。
- 未归档授权文件、CHM/CHW 文档、真实账号资料、商业脚本、签名私钥和 JSONL 运行数据。
