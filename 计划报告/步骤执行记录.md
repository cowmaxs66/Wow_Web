# 步骤执行记录

## P0-S01

### 步骤名称
建立项目根目录结构

### 目标说明
建立后续开发所需的模块化目录，保证项目从第一步开始可交接。

### 执行动作
- 创建计划报告、技术设计、实现模块、测试验证、发布归档目录。
- 为每个目录创建中文说明文档。

### 主要产物
- `README.md`
- `计划报告/README.md`
- `技术设计/README.md`
- `实现模块/README.md`
- `测试验证/README.md`
- `发布归档/README.md`

### 编译检查
文档步骤，无需编译。

### 调试/烟测
目录与说明文件可正常读取。

### 残留清理
未生成临时文件。

### 文档更新
已更新项目总阶段预览表、项目进度、变更记录。

### 下一步建议
完成最小 Rust workspace 并执行编译验证。

## P0-S02

### 步骤名称
建立项目规范与总方案

### 目标说明
把执行约束、阶段目标和 MVP 收敛决策固化到项目文档。

### 执行动作
- 创建 `AGENTS.md`。
- 创建 `计划报告/总体方案.md`。
- 创建 `技术设计/MVP收敛设计.md`。

### 主要产物
- `AGENTS.md`
- `计划报告/总体方案.md`
- `技术设计/MVP收敛设计.md`

### 编译检查
文档步骤，无需编译。

### 调试/烟测
文档结构可读，状态枚举统一。

### 残留清理
未生成临时文件。

### 文档更新
已更新项目进度、阶段 P0 步骤表、变更记录。

### 下一步建议
建立最小 Rust workspace。

## P0-S03

### 步骤名称
建立最小 Rust workspace

### 目标说明
建立 Client/Server 可复用的最小协议类型，并保证当前骨架可编译、可运行。

### 执行动作
- 创建根 `Cargo.toml` workspace。
- 创建 `shared-types` library。
- 创建 `client-agent` binary。
- 创建 `management-server` binary。
- 执行格式化、测试和烟测。

### 主要产物
- `Cargo.toml`
- `Cargo.lock`
- `实现模块/shared-types/src/lib.rs`
- `实现模块/client-agent/src/main.rs`
- `实现模块/management-server/src/main.rs`

### 编译检查
- `cargo fmt --all` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- `cargo run -p client-agent` 可输出标准状态 JSON。
- `cargo run -p management-server` 可输出协议契约样例 JSON。

### 残留清理
- 已执行 `cargo fmt --all`。
- 未保留临时测试文件。

### 文档更新
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/阶段P0-步骤表.md`
- 已更新：`测试验证/README.md`

### 下一步建议
初始化本地 git，并准备 GitHub 远端接入。

## P0-S05

### 步骤名称
初始化本地 git

### 目标说明
建立本地版本备份点，确保当前可验证成果可追溯、可回退、可交接。

### 执行动作
- 执行 `git init`。
- 设置默认分支为 `main`。
- 检查待提交文件和忽略文件。
- 创建首次本地提交。
- 增加 `.gitattributes` 固定文本文件换行规则。

### 主要产物
- `.git/`
- `.gitattributes`
- 本地提交 `c9a9c7e`

### 编译检查
- 本步骤未修改 Rust 逻辑。
- 前置验证 `cargo test --workspace` 已通过。

### 调试/烟测
- `git status` 已确认构建产物 `target/` 被忽略。
- 首次提交已成功创建。

### 残留清理
- 未提交 `target/`。
- 未生成临时文件。

### 文档更新
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/阶段P0-步骤表.md`
- 已更新：`计划报告/步骤执行记录.md`

### 下一步建议
接入 GitHub 远端仓库，完成远端备份。

## P0-S06

### 步骤名称
接入 GitHub 备份

### 目标说明
将本地 git 进度同步到 GitHub，形成远端备份和后续版本更新通道。

### 执行动作
- 检查本机 `gh` CLI。
- 检查 GitHub token 相关环境变量。
- 检查本地 git remote。
- 确认当前是否能直接创建或绑定远端仓库。
- 检查远端 `cowmaxs66/Wow_Web` 仓库状态。
- 绑定 `origin`。
- 推送本地 `main` 到 GitHub。

### 主要产物
- `计划报告/GitHub备份策略.md`
- `origin/main`
- GitHub 仓库 `https://github.com/cowmaxs66/Wow_Web`

### 编译检查
- 本步骤未修改 Rust 逻辑。
- 前置验证 `cargo test --workspace` 已通过。

### 调试/烟测
- `git log --oneline --decorate -5` 可看到本地提交。
- `git ls-remote https://github.com/cowmaxs66/Wow_Web.git` 初始为空，确认远端无历史。
- `git push -u origin main` 成功。
- `git remote -v` 可看到 `origin`。

### 残留清理
- 未创建临时文件。
- 未提交构建产物。

### 文档更新
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`
- 已记录：`计划报告/风险与问题记录.md` 中的 R001。
- 已更新：`计划报告/GitHub备份策略.md`

### 下一步建议
进入 P1，开始 Client Agent 配置读取与 Lua 宿主最小闭环。

## P1-S01

### 步骤名称
建立 Client 配置读取

### 目标说明
让 Client Agent 从本地 TOML 配置读取 `client.id` 和 Lua bootstrap 信息，移除入口中的硬编码 client_id。

### 执行动作
- 新增 `实现模块/client-agent/config/client-agent.toml`。
- 新增 `src/config/mod.rs`、`src/config/error.rs`、`src/config/path.rs`。
- 将配置读取、错误类型、默认路径解析拆分到不同文件。
- 在 Lua 可见配置中只暴露白名单键。

### 主要产物
- `实现模块/client-agent/config/client-agent.toml`
- `实现模块/client-agent/src/config/mod.rs`
- `实现模块/client-agent/src/config/error.rs`
- `实现模块/client-agent/src/config/path.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- 配置单元测试 `config_exposes_whitelisted_values` 通过。
- `cargo run -p client-agent` 输出的 `client_id` 来自 TOML 配置。

### 残留清理
- 未生成临时文件。
- 配置逻辑按职责拆分，未堆入入口文件。

### 文档更新
- 已更新：`计划报告/阶段P1-步骤表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`测试验证/README.md`
- 已更新：`实现模块/client-agent/README.md`

### 下一步建议
继续完成 Lua 宿主最小执行闭环。

## P1-S02

### 步骤名称
建立最小 Lua 宿主

### 目标说明
让 Client Agent 能启动 Lua 5.4 宿主，注册最小白名单 API，并执行开发期 bootstrap 脚本。

### 执行动作
- 引入 `mlua 0.12.0`，启用 `lua54` 和 `vendored`。
- 新增 `src/lua_host.rs`。
- 注册 Lua 函数 `log(message)`。
- 注册 Lua 函数 `get_config(key)`。
- 将 main 入口改为流程串接，不承载具体业务逻辑。

### 主要产物
- `实现模块/client-agent/src/lua_host.rs`
- `Cargo.toml`
- `Cargo.lock`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- Lua 单元测试 `lua_bootstrap_can_read_whitelisted_config` 通过。
- `cargo run -p client-agent` 输出 `current_script = inline-bootstrap`。
- Lua bootstrap 日志输出 `bootstrap started`。

### 残留清理
- 已执行 `cargo fmt --all`。
- 当前 client-agent 最大单文件为 60 行，入口文件 21 行。

### 文档更新
- 已更新：`AGENTS.md`
- 已更新：`README.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`
- 已更新：`计划报告/变更记录.md`

### 下一步建议
将 Lua bootstrap 从 TOML 内联脚本迁移到独立脚本文件，避免配置文件承载业务脚本。

## P1-S03

### 步骤名称
独立 Lua 脚本文件加载

### 目标说明
将 bootstrap 脚本从 TOML 内联内容迁移为独立 Lua 文件，保持配置只负责描述脚本名称和路径。

### 执行动作
- 新增 `实现模块/client-agent/scripts/bootstrap.lua`。
- 新增 `实现模块/client-agent/src/script/`。
- 将配置字段从 `bootstrap_script` 改为 `bootstrap_path`。
- 将相对脚本路径固定按 client-agent 模块根目录解析。

### 主要产物
- `实现模块/client-agent/scripts/bootstrap.lua`
- `实现模块/client-agent/src/script/mod.rs`
- `实现模块/client-agent/src/script/error.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `cargo run -p client-agent` 可读取 `scripts/bootstrap.lua`。
- 输出状态 JSON 中 `current_script = bootstrap`。

### 残留清理
- 已执行 `cargo fmt --all`。
- 未生成临时脚本文件。

### 文档更新
- 已更新：`计划报告/阶段P1-步骤表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`实现模块/client-agent/README.md`
- 已更新：`技术设计/ClientAgent最小闭环设计.md`

### 下一步建议
为 Lua 宿主增加指令上限和错误边界。

## P1-S04

### 步骤名称
脚本停止与错误边界

### 目标说明
为 Lua 脚本执行增加最小安全边界，避免无限循环脚本卡死 Client Agent。

### 执行动作
- 在配置中新增 `lua.instruction_limit`。
- 在 `LuaHost` 中通过 mlua VM hook 设置指令上限。
- 增加无限循环脚本单元测试。

### 主要产物
- `实现模块/client-agent/src/lua_host.rs`
- `实现模块/client-agent/config/client-agent.toml`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- 测试 `lua_bootstrap_reports_instruction_limit_error` 通过。
- 无限循环脚本会返回 `Lua 脚本超过指令上限`。

### 残留清理
- 未保留临时错误脚本。
- 指令限制逻辑集中在 Lua 宿主模块，未污染入口文件。

### 文档更新
- 已更新：`计划报告/阶段P1-步骤表.md`
- 已更新：`测试验证/README.md`
- 已更新：`技术设计/ClientAgent最小闭环设计.md`

### 下一步建议
建立 Client Agent 本地状态模型。

## P1-S05

### 步骤名称
Client 本地状态模型

### 目标说明
将 Lua 执行结果转换为 Client 状态输出，避免 `main.rs` 直接拼装共享协议结构。

### 执行动作
- 新增 `实现模块/client-agent/src/status.rs`。
- 新增 `ScriptRunReport` 作为 Lua 执行报告。
- 将状态构造迁移到 `AgentStatusSnapshot`。

### 主要产物
- `实现模块/client-agent/src/status.rs`
- `实现模块/client-agent/src/lua_host.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `cargo run -p client-agent` 输出在线状态。
- 输出 JSON 中 `data.current_script = bootstrap`。

### 残留清理
- 入口文件保持流程串接职责。
- 未新增未调用状态函数。

### 文档更新
- 已更新：`实现模块/client-agent/README.md`
- 已更新：`技术设计/ClientAgent最小闭环设计.md`
- 已更新：`计划报告/项目进度.md`

### 下一步建议
接入本地结构化日志。

## P1-S06

### 步骤名称
本地日志结构化

### 目标说明
将 Lua `log(message)` 输出接入 Rust `tracing`，为后续状态、任务和大漠调用日志建立统一入口。

### 执行动作
- 在 workspace 中新增 `tracing` 和 `tracing-subscriber`。
- 新增 `实现模块/client-agent/src/logging.rs`。
- 将 Lua `log(message)` 改为 `tracing::info!` 输出。

### 主要产物
- `实现模块/client-agent/src/logging.rs`
- `Cargo.toml`
- `Cargo.lock`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `cargo run -p client-agent` 可正常启动。
- Lua 日志不再直接散落到 `eprintln!`。

### 残留清理
- 已执行 `cargo fmt --all`。
- 未引入额外日志配置文件。

### 文档更新
- 已更新：`实现模块/client-agent/README.md`
- 已更新：`计划报告/变更记录.md`

### 下一步建议
整理 P1 测试与烟测记录。

## P1-S07

### 步骤名称
P1 验证脚本与测试整理

### 目标说明
完成 P1 本地最小闭环的编译、单元测试、启动烟测和文档记录。

### 执行动作
- 执行 `cargo fmt --all`。
- 执行 `cargo test --workspace`。
- 执行 `cargo run -p client-agent`。
- 检查 client-agent 单文件行数。
- 更新测试验证与阶段记录。

### 主要产物
- `测试验证/README.md`
- `计划报告/阶段P1-步骤表.md`
- `计划报告/项目进度.md`

### 编译检查
- `cargo test --workspace` 通过。
- client-agent 4 项测试通过。
- shared-types 1 项测试通过。

### 调试/烟测
- `cargo run -p client-agent` 通过。
- 输出 `client_id = local-dev-client`。
- 输出 `current_script = bootstrap`。

### 残留清理
- 未提交 `target/`。
- 未生成临时测试文件。

### 文档更新
- 已更新：`测试验证/README.md`
- 已更新：`计划报告/阶段P1-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`

### 下一步建议
提交并推送 P1 可用进度，然后进入 P2-S04。

## P1-S08

### 步骤名称
P1 阶段收口

### 目标说明
完成 Client Agent 本地最小闭环的版本备份和阶段归档。

### 执行动作
- 提交 P1 Client Agent 脚本运行时改动。
- 推送 `main` 到 GitHub。
- 将 P1 阶段表更新为 8/8。

### 主要产物
- Git 提交 `059d235 feat: complete client agent script runtime`
- GitHub `origin/main`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `cargo run -p client-agent` 通过。

### 残留清理
- 未提交 `target/`。
- 未提交大漠 DLL、CHM、授权资料。

### 文档更新
- 已更新：`计划报告/阶段P1-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`

### 下一步建议
进入 P2 大漠桥接验证收口。

## P2-S01

### 步骤名称
大漠资料盘点与接口范围收敛

### 目标说明
根据用户提供的大漠 7.2149 本机资料，先确定第一版 DmBridge 需要覆盖哪些功能域，避免后续桥接层盲目暴露全部大漠接口。

### 执行动作
- 盘点 `I:/图色/工具/大漠/7.2149` 目录。
- 读取 Delphi 类库 `Output/Delphi/obj.pas`。
- 使用 `hh.exe` 将 CHM 解包到临时目录。
- 提取窗口、后台设置、基本设置、算法、图色、键鼠六类接口清单。
- 明确第一版必接、延后、排除接口。
- 更新 `.gitignore`，防止误提交 CHM、CHW、RAR、DAT 和私有资料目录。

### 主要产物
- `技术设计/大漠接口范围设计.md`
- `计划报告/阶段P2-步骤表.md`
- `.gitignore`

### 编译检查
- 本步骤未修改 Rust 逻辑。
- 需要执行 `cargo test --workspace` 作为回归检查。

### 调试/烟测
- CHM 解包成功。
- 目标分类接口提取成功：
  - 窗口：39 项
  - 后台设置：28 项
  - 基本设置：16 个可调用方法 + 1 个说明页
  - 算法：3 项
  - 图色：56 项
  - 键鼠：34 个可调用方法 + 1 个键码表

### 残留清理
- CHM 解包目录位于系统临时目录，未写入项目仓库。
- 未复制 `dm.dll`、`RegDll.dll`、CHM、CHW 到 GitHub 仓库。

### 文档更新
- 已更新：`技术设计/README.md`
- 已更新：`计划报告/README.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`
- 已更新：`计划报告/风险与问题记录.md`
- 已更新：`计划报告/变更记录.md`

### 下一步建议
进入 P2-S02，设计 DmBridge C ABI 导出函数清单和字符串内存规范。

## P2-S02

### 步骤名称
DmBridge C ABI 契约设计

### 目标说明
在实现 Delphi DLL 前，先固定 Rust 与 DmBridge 的调用边界，包括导出函数、字符串规范、错误码、结果结构、线程模型和目录排版。

### 执行动作
- 新增 `技术设计/DmBridge_C_ABI契约设计.md`。
- 定义 `stdcall` 调用约定。
- 定义 UTF-16 输入输出规范。
- 定义 Bridge 状态码与大漠返回值分离规则。
- 定义 `DmBridgePoint`、`DmBridgeRect`、`DmBridgeSize`、`DmBridgeFindResult`。
- 定义生命周期、基本设置、窗口、后台、算法、图色、键鼠导出函数清单。
- 定义 P2 最小烟测链路。
- 定义 Delphi 和 Rust 两侧目录排版。

### 主要产物
- `技术设计/DmBridge_C_ABI契约设计.md`
- `实现模块/dm-bridge/README.md`

### 编译检查
- 本步骤未修改 Rust 逻辑。
- 需要执行 `cargo test --workspace` 作为回归检查。

### 调试/烟测
- 契约可覆盖 P2 最小链路：
  `abi_version -> init -> ver -> set_path -> find_window -> bind_window -> get_color -> move_to -> left_click -> unbind_window -> shutdown`

### 残留清理
- 未创建 Delphi 占位工程。
- 未复制大漠 DLL、CHM、授权资料。

### 文档更新
- 已更新：`技术设计/README.md`
- 已更新：`实现模块/dm-bridge/README.md`
- 已更新：`计划报告/阶段P2-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`
- 已更新：`计划报告/变更记录.md`
- 已更新：`计划报告/风险与问题记录.md`

### 下一步建议
进入 P2-S03，按契约建立 Delphi `DmBridge.dll` 最小工程，只实现 P2 最小烟测链路。

## P2-S03

### 步骤名称
Delphi DmBridge 最小工程

### 目标说明
按 P2-S02 的 C ABI 契约建立可编译的 Delphi DLL 工程，先实现大漠烟测链路所需导出函数。

### 执行动作
- 检查 Delphi 编译器路径 `C:\RAD13\bin\dcc32.exe`。
- 建立 `实现模块/dm-bridge/src/`。
- 新增 `DmBridge.dpr`、`Types`、`Strings`、`Errors`、`Dmsoft`、`Worker` 和分领域 `Api.*` 单元。
- 新增 `实现模块/dm-bridge/build.ps1`。
- 使用 `dcc32` 编译 Win32 DLL。
- 使用 `tdump` 检查 `dm_bridge_*` 导出符号。

### 主要产物
- `实现模块/dm-bridge/build.ps1`
- `实现模块/dm-bridge/src/DmBridge.dpr`
- `实现模块/dm-bridge/src/DmBridge.Types.pas`
- `实现模块/dm-bridge/src/DmBridge.Strings.pas`
- `实现模块/dm-bridge/src/DmBridge.Errors.pas`
- `实现模块/dm-bridge/src/DmBridge.Dmsoft.pas`
- `实现模块/dm-bridge/src/DmBridge.Worker.pas`
- `实现模块/dm-bridge/src/DmBridge.Api.Common.pas`
- `实现模块/dm-bridge/src/DmBridge.Api.Lifecycle.pas`
- `实现模块/dm-bridge/src/DmBridge.Api.Basic.pas`
- `实现模块/dm-bridge/src/DmBridge.Api.Window.pas`
- `实现模块/dm-bridge/src/DmBridge.Api.Bind.pas`
- `实现模块/dm-bridge/src/DmBridge.Api.Color.pas`
- `实现模块/dm-bridge/src/DmBridge.Api.Input.pas`
- 构建产物：`target/dm-bridge/Win32/DmBridge.dll`，不提交

### 编译检查
- `.\实现模块\dm-bridge\build.ps1` 通过。
- `cargo test --workspace` 需要作为回归检查。

### 调试/烟测
- `tdump -ee target/dm-bridge/Win32/DmBridge.dll` 可看到以下导出：
  - `dm_bridge_abi_version`
  - `dm_bridge_init`
  - `dm_bridge_shutdown`
  - `dm_bridge_ver`
  - `dm_bridge_set_path`
  - `dm_bridge_find_window`
  - `dm_bridge_bind_window`
  - `dm_bridge_get_color`
  - `dm_bridge_move_to`
  - `dm_bridge_left_click`
  - `dm_bridge_unbind_window`
  - `dm_bridge_get_last_bridge_error`
  - `dm_bridge_get_last_dm_error`
  - `dm_bridge_get_last_message`

### 残留清理
- DLL、DCU、MAP、TDS 等构建产物输出到 `target/`，不提交。
- 未复制 `dm.dll`、`RegDll.dll`、CHM、授权资料。

### 文档更新
- 已更新：`实现模块/dm-bridge/README.md`
- 已更新：`技术设计/DmBridge_C_ABI契约设计.md`
- 已更新：`计划报告/阶段P2-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`
- 已更新：`计划报告/变更记录.md`
- 已更新：`计划报告/风险与问题记录.md`

### 下一步建议
进入 P2-S04 前，先把当前最小直接 STA 模式升级为真正 STA Worker 队列，再建立 Rust `libloading` 调用验证。

## P2-S04

### 步骤名称
DmBridge STA Worker 队列

### 目标说明
将 DmBridge 从“初始化线程直接调用”升级为内部 STA Worker 队列，保证 Rust 后续可以从任意线程调用导出函数，而大漠 COM 始终在同一个 STA 线程内执行。

### 执行动作
- 拆分 Worker 类型、请求对象、线程队列和对外门面。
- 新增 `DmBridge.Worker.Types.pas`。
- 新增 `DmBridge.Worker.Request.pas`。
- 新增 `DmBridge.Worker.Thread.pas`。
- 重写 `DmBridge.Worker.pas` 为门面函数。
- 更新 `DmBridge.dpr` 单元引用。
- 使用 RAD13 `dcc32.exe` 编译 Win32 DLL。
- 使用 `tdump` 检查导出符号。

### 主要产物
- `实现模块/dm-bridge/src/DmBridge.Worker.Types.pas`
- `实现模块/dm-bridge/src/DmBridge.Worker.Request.pas`
- `实现模块/dm-bridge/src/DmBridge.Worker.Thread.pas`
- `实现模块/dm-bridge/src/DmBridge.Worker.pas`
- `实现模块/dm-bridge/src/DmBridge.dpr`

### 编译检查
- `.\实现模块\dm-bridge\build.ps1` 通过。

### 调试/烟测
- `tdump -ee target\dm-bridge\Win32\DmBridge.dll` 可看到完整 `dm_bridge_*` 导出函数。
- 导出函数名称与 P2-S03 保持一致。

### 残留清理
- DLL、DCU、MAP、TDS 等构建产物仍输出到 `target/`，不提交。
- 未复制 `dm.dll`、`RegDll.dll`、CHM、授权资料。
- Worker 代码已按职责拆分，最大单文件 174 行。

### 文档更新
- 已更新：`计划报告/阶段P2-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`
- 已更新：`计划报告/风险与问题记录.md`
- 已更新：`计划报告/变更记录.md`
- 已更新：`实现模块/dm-bridge/README.md`
- 已更新：`技术设计/DmBridge_C_ABI契约设计.md`
- 已更新：`测试验证/README.md`

### 下一步建议
进入 P2-S05，建立 Rust `libloading` 模块并验证 ABI 版本读取。

## P2-S05

### 步骤名称
Rust libloading 调用验证

### 目标说明
在 Rust client-agent 中建立 DmBridge 安全封装，验证 32 位 Rust 进程可以加载 Win32 `DmBridge.dll` 并读取 ABI 版本。

### 执行动作
- 新增 `实现模块/client-agent/src/dm_bridge/`。
- 新增 `ffi.rs`、`error.rs`、`strings.rs`、`path.rs`、`mod.rs`。
- 引入 `libloading`。
- 增加 PE Machine 位数检查。
- 增加 `DM_BRIDGE_DLL` 环境变量烟测。

### 主要产物
- `实现模块/client-agent/src/dm_bridge/ffi.rs`
- `实现模块/client-agent/src/dm_bridge/error.rs`
- `实现模块/client-agent/src/dm_bridge/strings.rs`
- `实现模块/client-agent/src/dm_bridge/path.rs`
- `实现模块/client-agent/src/dm_bridge/mod.rs`

### 编译检查
- `cargo test --workspace` 通过。
- `cargo test -p client-agent --target i686-pc-windows-msvc dm_bridge_loads_abi_version_from_env_when_available -- --nocapture` 通过。

### 调试/烟测
- `dm_bridge_abi_version` 返回 `1`。
- 已修复 Delphi 内部常量与导出函数同名导致的递归栈溢出。

### 残留清理
- 未提交 `target/`。
- 未复制或提交 `dm.dll`。

### 文档更新
- 已更新：`测试验证/README.md`
- 已更新：`技术设计/DmBridge_C_ABI契约设计.md`
- 已更新：`计划报告/阶段P2-步骤表.md`

### 下一步建议
将 DmBridge 封装注册为 Lua `dm` 高层 API。

## P2-S06

### 步骤名称
Lua 暴露 dm 高层 API

### 目标说明
让 Lua 脚本通过受控高层 API 调用大漠能力，而不是直接接触 C ABI 或裸指针。

### 执行动作
- 新增 `实现模块/client-agent/src/lua_dm.rs`。
- 在 `LuaHost` 中注册全局 `dm` 表。
- 暴露 `abi_version/init/shutdown/ver/set_path/find_window/bind_window/get_color/move_to/left_click/unbind_window/last_bridge_error/last_dm_error`。
- 使用懒加载，只在 Lua 调用 `dm.*` 时加载 DmBridge。

### 主要产物
- `实现模块/client-agent/src/lua_dm.rs`
- `实现模块/client-agent/src/lua_host.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `lua_dm_api_reads_abi_version_from_env_when_available` 在 32 位 target 下通过。
- Lua 不直接访问 `libloading::Symbol` 或 C ABI 指针。

### 残留清理
- Lua API 注册从 `lua_host.rs` 拆出，避免宿主文件继续膨胀。

### 文档更新
- 已更新：`实现模块/client-agent/README.md`
- 已更新：`技术设计/ClientAgent最小闭环设计.md`
- 已更新：`技术设计/DmBridge_C_ABI契约设计.md`

### 下一步建议
执行图色与键鼠最小烟测。

## P2-S07

### 步骤名称
图色与键鼠链路烟测

### 目标说明
验证 DmBridge 的最小大漠 COM 链路可从 Rust 和 Lua 侧实际调用。

### 执行动作
- 查询 32 位 COM 中 `dm.dmsoft` 已注册。
- 使用 32 位 P/Invoke 验证 `init/ver/get_color/move_to/shutdown`。
- 使用 32 位 Rust 测试验证同一链路。
- 使用 32 位 Lua 高层 API 测试验证同一链路。

### 主要产物
- Rust 可选烟测 `dm_bridge_com_ver_and_color_smoke_when_enabled`。
- Lua 可选烟测 `lua_dm_api_com_ver_and_color_smoke_when_enabled`。

### 编译检查
- `cargo test --workspace` 通过。
- `.\实现模块\dm-bridge\build.ps1` 通过。

### 调试/烟测
- 32 位 P/Invoke：`Ver = 7.2149`，`GetColor(0,0) = 000000`，`MoveTo(0,0)` 返回 `1`。
- 32 位 Rust COM 烟测通过。
- 32 位 Lua COM 烟测通过。
- 未自动执行 `LeftClick`，避免误点击。

### 残留清理
- 已删除 `target/dm-bridge` 下的临时探针文件。
- 已更新 build 脚本，每次编译前清理 DCU 目录，避免旧单元残留。

### 文档更新
- 已更新：`测试验证/README.md`
- 已更新：`计划报告/风险与问题记录.md`
- 已更新：`计划报告/项目进度.md`

### 下一步建议
完成 P2 阶段收口并推送 GitHub。

## P2-S08

### 步骤名称
P2 阶段收口

### 目标说明
完成大漠桥接验证阶段的文档同步、残留清理、版本备份和远端推送。

### 执行动作
- 更新 P2 阶段表、总阶段预览、项目进度、测试记录、风险记录和变更记录。
- 检查 git 变更范围。
- 确认未提交 `target/`、`dm.dll`、`RegDll.dll`、CHM、授权资料。
- 准备提交并推送 `main`。

### 主要产物
- P2 收口文档。
- P2 本地可用代码。

### 编译检查
- `cargo test --workspace` 通过。
- `.\实现模块\dm-bridge\build.ps1` 通过。

### 调试/烟测
- `cargo run -p client-agent` 通过。
- 32 位 Rust DmBridge ABI 烟测通过。
- 32 位 Rust/Lua COM 烟测通过。

### 残留清理
- `target/` 保持忽略状态。
- 临时探针文件已删除。
- build 脚本会清理旧 DCU。

### 文档更新
- 已更新：`计划报告/阶段P1-步骤表.md`
- 已更新：`计划报告/阶段P2-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`
- 已更新：`计划报告/变更记录.md`
- 已更新：`计划报告/风险与问题记录.md`
- 已更新：`测试验证/README.md`
- 已更新：`实现模块/client-agent/README.md`
- 已更新：`实现模块/dm-bridge/README.md`
- 已更新：`技术设计/ClientAgent最小闭环设计.md`
- 已更新：`技术设计/DmBridge_C_ABI契约设计.md`

### 下一步建议
进入 P3：Client Agent 与 Management Server 通讯闭环。

## P3-S01

### 步骤名称
P3 通讯边界设计

### 目标说明
固定 P3 只做本地状态上报闭环，避免同时引入 WebSocket、鉴权、数据库和 Web 管理端导致阶段失控。

### 执行动作
- 新增 `技术设计/Server通讯闭环设计.md`。
- 新增 `计划报告/阶段P3-步骤表.md`。
- 定义 `GET /health`、`POST /api/client/status`、`GET /api/client/status/{client_id}`。

### 主要产物
- `技术设计/Server通讯闭环设计.md`
- `计划报告/阶段P3-步骤表.md`

### 编译检查
文档步骤，无需编译。

### 调试/烟测
文档结构可读，阶段边界明确。

### 残留清理
未生成临时文件。

### 文档更新
- 已更新：`计划报告/README.md`
- 已更新：`技术设计/README.md`
- 已更新：`计划报告/项目总阶段预览表.md`

### 下一步建议
扩展 shared-types 的通讯响应类型。

## P3-S02

### 步骤名称
扩展 shared-types 通讯响应

### 目标说明
增加 Server 健康检查和状态上报 ACK 所需的共享响应类型。

### 执行动作
- 新增 `HealthResponse`。
- 新增 `StatusAck`。
- 保持原有 `WsEnvelope<ClientStatus>` 不变。

### 主要产物
- `实现模块/shared-types/src/lib.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `status_ack_keeps_message_identity` 测试通过。

### 残留清理
未引入无用类型。

### 文档更新
- 已更新：`技术设计/Server通讯闭环设计.md`
- 已更新：`测试验证/README.md`

### 下一步建议
实现 Management Server HTTP API。

## P3-S03

### 步骤名称
Management Server HTTP API

### 目标说明
让 Management Server 可以启动本地 HTTP 服务，接收并查询 Client 最新状态。

### 执行动作
- 引入 `axum` 和 `tokio`。
- 新增 `src/config.rs`。
- 新增 `src/state.rs`。
- 新增 `src/error.rs`。
- 新增 `src/app.rs`。
- 将 `src/main.rs` 改为 Axum Server 启动入口。

### 主要产物
- `实现模块/management-server/src/main.rs`
- `实现模块/management-server/src/config.rs`
- `实现模块/management-server/src/state.rs`
- `实现模块/management-server/src/error.rs`
- `实现模块/management-server/src/app.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `status_report_can_be_queried_back` 测试通过。
- `mismatched_client_id_is_rejected` 测试通过。

### 残留清理
Server 状态只保存在内存中，不创建临时数据库文件。

### 文档更新
- 已更新：`实现模块/management-server/README.md`
- 已更新：`测试验证/README.md`

### 下一步建议
实现 Client 状态上报模块。

## P3-S04

### 步骤名称
Client 状态上报模块

### 目标说明
让 Client Agent 在生成本地状态后，可选上报到 Management Server。

### 执行动作
- 配置新增 `[server]` 段。
- 新增 `src/server_reporter.rs`。
- 增加 `CLIENT_AGENT_SERVER_ENABLED` 等环境变量覆盖。
- 在 `main.rs` 中按配置决定是否上报。

### 主要产物
- `实现模块/client-agent/src/server_reporter.rs`
- `实现模块/client-agent/config/client-agent.toml`
- `实现模块/client-agent/src/config/mod.rs`
- `实现模块/client-agent/src/main.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `status_reporter_posts_status_and_reads_ack` 测试通过。
- 默认 `cargo run -p client-agent` 不依赖 Server。

### 残留清理
未引入 HTTP 客户端大框架，P3 只保留本机最小 HTTP 上报逻辑。

### 文档更新
- 已更新：`实现模块/client-agent/README.md`
- 已更新：`技术设计/Server通讯闭环设计.md`

### 下一步建议
执行本地集成烟测。

## P3-S05

### 步骤名称
本地集成烟测

### 目标说明
验证真实 Server 进程和真实 Client 进程之间可以完成状态上报和查询。

### 执行动作
- 使用 `Start-Process -WindowStyle Hidden` 启动 `cargo run -p management-server`。
- 轮询 `GET /health` 等待 Server 就绪。
- 设置 `CLIENT_AGENT_SERVER_ENABLED=1`。
- 执行 `cargo run -p client-agent`。
- 查询 `GET /api/client/status/local-dev-client`。

### 主要产物
- P3 本地通讯烟测结果。

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- `GET /health` 返回 `ok`。
- Server 查询结果包含 `client_id = local-dev-client`。
- Server 查询结果包含 `current_script = bootstrap`。
- Server 查询结果包含 `online = true`。

### 残留清理
- 已停止后台 Server 进程。
- 已删除 `target/p3-smoke` 临时日志目录。

### 文档更新
- 已更新：`测试验证/README.md`
- 已更新：`计划报告/项目进度.md`

### 下一步建议
整理错误边界与日志。

## P3-S06

### 步骤名称
错误边界与日志整理

### 目标说明
保证 Client 上报失败时有明确错误，不吞错、不假成功。

### 执行动作
- 增加 HTTP 响应解析错误类型。
- 支持 CRLF/LF 响应分隔。
- 支持最小 chunked body 解码。
- 移除 TCP 写端 half-close，兼容 Axum/Hyper 真实响应。
- Server 增加 400/404 API 错误响应。

### 主要产物
- `实现模块/client-agent/src/server_reporter.rs`
- `实现模块/management-server/src/error.rs`
- `实现模块/management-server/src/app.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- 真实 P3 集成烟测通过。
- `parse_status_ack_accepts_chunked_response` 测试通过。

### 残留清理
临时调试日志未保留。

### 文档更新
- 已更新：`计划报告/风险与问题记录.md`
- 已记录：R012。

### 下一步建议
整理 P3 文档与测试记录。

## P3-S07

### 步骤名称
P3 文档与测试整理

### 目标说明
同步 P3 的过程记录、结构状态、测试结果和模块说明。

### 执行动作
- 更新 P3 阶段表。
- 更新项目总阶段预览表。
- 更新项目进度。
- 更新测试验证记录。
- 更新 Client/Server README。

### 主要产物
- `计划报告/阶段P3-步骤表.md`
- `计划报告/项目进度.md`
- `测试验证/README.md`
- `实现模块/client-agent/README.md`
- `实现模块/management-server/README.md`

### 编译检查
- 文档步骤前置验证 `cargo test --workspace` 已通过。

### 调试/烟测
- P3 本地通讯烟测已通过。

### 残留清理
未生成临时文档草稿。

### 文档更新
- 已更新双 MD。

### 下一步建议
提交并推送 P3 可用版本。

## P3-S08

### 步骤名称
P3 阶段收口

### 目标说明
完成 P3 的版本备份和远端同步。

### 执行动作
- 检查 git 变更范围。
- 拆分 Client `server_reporter`，将错误类型和 HTTP 响应解析移入独立子模块。
- 执行最终测试。
- 执行最终本地通讯烟测。
- 提交 P3 代码和文档。
- 推送 `main` 到 GitHub。

### 主要产物
- P3 本地通讯闭环提交。
- `实现模块/client-agent/src/server_reporter.rs`
- `实现模块/client-agent/src/server_reporter/error.rs`
- `实现模块/client-agent/src/server_reporter/response.rs`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- P3 本地集成烟测通过：`GET /health`、Client 上报、`GET /api/client/status/local-dev-client` 均通过。
- Server 查询结果包含 `message_type = status`、`current_script = bootstrap`、`online = true`。

### 残留清理
- `target/` 未提交。
- `target/p3-smoke` 已删除。

### 文档更新
- 已更新：`计划报告/步骤执行记录.md`
- 已更新：`计划报告/阶段P3-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`实现模块/client-agent/README.md`
- 已更新：`技术设计/Server通讯闭环设计.md`

### 下一步建议
进入 P4 前，先确认 Web 管理端需要读取哪些 Server API。

## P4-S01

### 步骤名称
P4 范围与 UI 边界设计

### 目标说明
固定 Web 管理端 MVP 的 API 边界和 UI 范围，避免直接做脚本下发、日志系统、鉴权等未成熟能力。

### 执行动作
- 新增 P4 阶段步骤表。
- 新增 Web 管理端 MVP 技术设计。
- 明确 P4 只读取 Server 健康状态和 Client 最新状态。
- 明确 Web Admin 使用 Vue/Vite。

### 主要产物
- `计划报告/阶段P4-步骤表.md`
- `技术设计/Web管理端MVP设计.md`

### 编译检查
- 文档步骤，无需编译。

### 调试/烟测
- 文档结构可读，阶段状态已同步。

### 残留清理
- 未生成临时文件。

### 文档更新
- 已更新：`计划报告/README.md`
- 已更新：`技术设计/README.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/变更记录.md`

### 下一步建议
初始化 Vue/Vite Web 管理端工程。

## P4-S02

### 步骤名称
Vue/Vite 工程初始化

### 目标说明
在既定 `web-admin` 目录下建立 Vue/Vite/TypeScript 前端工程，避免继续停留在空目录说明状态。

### 执行动作
- 创建 `package.json`、`package-lock.json`、`index.html`、`vite.config.ts`、`tsconfig.json`。
- 创建 `src/main.ts`。
- 创建 `src/styles/tokens.css` 和 `src/styles/base.css`。
- 安装 Vue、Vite、TypeScript 和 `@lucide/vue`。
- 补充 `.gitignore` 的 `coverage/`。

### 主要产物
- `实现模块/web-admin/package.json`
- `实现模块/web-admin/package-lock.json`
- `实现模块/web-admin/src/main.ts`
- `实现模块/web-admin/src/styles/`

### 编译检查
- `npm install` 通过，无漏洞。

### 调试/烟测
- 前端依赖解析正常。

### 残留清理
- `node_modules/` 已被 `.gitignore` 排除。
- `dist/` 已被 `.gitignore` 排除。

### 文档更新
- 已更新：`实现模块/web-admin/README.md`

### 下一步建议
补充 Management Server 给 Web Admin 读取的 API。

## P4-S03

### 步骤名称
Management Server Web API 补充

### 目标说明
为 Web Admin 提供状态列表 API，并允许本地 Vite 页面访问 Server。

### 执行动作
- 新增 `GET /api/client/status`。
- 为 `ServerState` 增加 `list_statuses`。
- 列表结果按 `client_id` 排序，保证 UI 与测试顺序稳定。
- 增加开发期 CORS。
- 增加状态列表单元测试。

### 主要产物
- `实现模块/management-server/src/app.rs`
- `实现模块/management-server/src/state.rs`
- `Cargo.toml`
- `Cargo.lock`

### 编译检查
- `cargo fmt --all` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- P4 API 烟测通过：`GET /api/client/status` 返回 `local-dev-client`。

### 残留清理
- `target/p4-smoke` 临时目录已清理。

### 文档更新
- 已更新：`实现模块/management-server/README.md`
- 已更新：`计划报告/风险与问题记录.md`

### 下一步建议
实现 Web Admin API 客户端。

## P4-S04

### 步骤名称
Web API 客户端封装

### 目标说明
让 Web Admin 通过明确类型和统一错误处理调用 Management Server。

### 执行动作
- 新增前端协议类型。
- 新增 `ManagementServerError`。
- 新增 `fetchHealth`、`fetchClientStatuses`、`fetchClientStatus`。
- 增加 Server URL 最小校验。

### 主要产物
- `实现模块/web-admin/src/types/protocol.ts`
- `实现模块/web-admin/src/api/managementServer.ts`

### 编译检查
- `npm run build` 通过。

### 调试/烟测
- Playwright 浏览器验证中成功读取真实 API 数据。

### 残留清理
- 无临时文件。

### 文档更新
- 已更新：`技术设计/Web管理端MVP设计.md`

### 下一步建议
实现 Web 管理端主界面。

## P4-S05

### 步骤名称
Web 管理端主界面

### 目标说明
完成浏览器可用的状态看板，展示 Server 健康、Client 列表、连接设置和状态详情。

### 执行动作
- 新增 App Shell 和侧边导航。
- 新增指标卡、状态点、客户端列表、连接设置、状态详情组件。
- 新增 `useDashboardStatus`，将刷新流程从 `App.vue` 拆出。
- 移动端将表格改为字段卡片，避免窄屏内容截断。

### 主要产物
- `实现模块/web-admin/src/App.vue`
- `实现模块/web-admin/src/composables/useDashboardStatus.ts`
- `实现模块/web-admin/src/components/`

### 编译检查
- `npm run build` 通过。

### 调试/烟测
- 页面可显示 `Server 正常`、`local-dev-client`、`bootstrap`。
- 点击刷新按钮可重新请求 API。

### 残留清理
- `dist/` 构建产物未提交。

### 文档更新
- 已更新：`实现模块/web-admin/README.md`

### 下一步建议
执行三端联调和浏览器截图验证。

## P4-S06

### 步骤名称
本地联调烟测

### 目标说明
验证 Server、Client、Web Admin 三端闭环真实可用。

### 执行动作
- 启动 `management-server.exe`。
- 运行 `client-agent.exe` 并启用状态上报。
- 启动 Vite Web Admin。
- 使用 Playwright fallback 进行桌面和移动端验证。
- 使用 `view_image` 对照概念图和最终截图。

### 主要产物
- P4 API 烟测结果。
- Playwright 桌面和移动端验证结果。

### 编译检查
- `npm run build` 通过。
- `cargo fmt --all` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- API 烟测通过：状态列表返回 `local-dev-client`。
- 桌面 1440x920：无横向溢出，真实状态显示正常。
- 移动 390x844：无横向溢出，客户端状态字段完整显示。

### 残留清理
- 已停止临时 Server 和 Vite 进程。
- 已删除 `target/p4-browser` 临时截图目录。
- 已删除 `实现模块/web-admin/dist`。

### 文档更新
- 已更新：`测试验证/README.md`

### 下一步建议
整理 P4 文档与版本收口。

## P4-S07

### 步骤名称
文档与测试记录整理

### 目标说明
同步 P4 的模块说明、测试记录、风险和当前进度。

### 执行动作
- 更新 P4 阶段步骤表。
- 更新项目总阶段预览表。
- 更新项目进度。
- 更新测试验证记录。
- 更新 Web Admin 和 Management Server README。
- 记录开发期 CORS 风险。

### 主要产物
- `计划报告/阶段P4-步骤表.md`
- `计划报告/项目进度.md`
- `测试验证/README.md`
- `实现模块/web-admin/README.md`
- `实现模块/management-server/README.md`

### 编译检查
- 文档步骤前置验证已通过。

### 调试/烟测
- P4 本地联调烟测已通过。

### 残留清理
- 未保留临时截图或构建产物。

### 文档更新
- 已更新双 MD。

### 下一步建议
提交并推送 P4 可用版本。

## P4-S08

### 步骤名称
P4 阶段收口

### 目标说明
完成 P4 的版本备份和远端同步。

### 执行动作
- 检查 git 变更范围。
- 执行最终格式、构建、clippy、测试检查。
- 提交 P4 代码和文档。
- 推送 `main` 到 GitHub。

### 主要产物
- P4 Web 管理端 MVP 提交。

### 编译检查
- `npm run build` 通过。
- `cargo fmt --all` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- P4 本地联调烟测已通过。
- 最终 Playwright fallback 桌面和移动端验证通过。
- 最终 `view_image` 已检查概念图、桌面截图和移动截图。

### 残留清理
- `node_modules/` 未提交。
- `dist/` 未提交。
- `target/` 未提交。
- 临时 Server 与 Vite 进程已停止。
- `target/p4-browser-final` 已删除。

### 文档更新
- 已更新：`计划报告/步骤执行记录.md`

### 下一步建议
进入 P5 前，先确认脚本管理与安全机制边界。

## P5-S01

### 步骤名称
P5 安全边界设计

### 目标说明
固定 P5 只做本机脚本执行前安全门，不进入远程下发、Web 扩展、数据库脚本仓库和签名工具链。

### 执行动作
- 新增 P5 阶段步骤表。
- 新增脚本管理与安全机制设计。
- 明确 manifest、签名、hash、权限白名单的执行顺序。
- 明确 P5 不提交签名私钥。

### 主要产物
- `计划报告/阶段P5-步骤表.md`
- `技术设计/脚本管理与安全机制设计.md`

### 编译检查
- 文档步骤，无需编译。

### 调试/烟测
- 文档结构可读，阶段状态已同步。

### 残留清理
- 未生成临时文件。

### 文档更新
- 已更新：`计划报告/README.md`
- 已更新：`技术设计/README.md`

### 下一步建议
建立脚本 manifest 格式。

## P5-S02

### 步骤名称
脚本 manifest 格式

### 目标说明
为 bootstrap Lua 建立可验证 manifest，记录版本、入口、hash、权限和签名。

### 执行动作
- 新增 `scripts/bootstrap.manifest.json`。
- 新增 `script_security` TOML 配置。
- 配置 manifest 路径、可信 Ed25519 公钥和允许权限。
- manifest entry 固定为相对路径。

### 主要产物
- `实现模块/client-agent/scripts/bootstrap.manifest.json`
- `实现模块/client-agent/config/client-agent.toml`

### 编译检查
- `cargo test -p client-agent` 通过。

### 调试/烟测
- 默认 `cargo run -p client-agent` 可读取 manifest 并运行 bootstrap。

### 残留清理
- 未提交签名私钥。

### 文档更新
- 已更新：`实现模块/client-agent/README.md`

### 下一步建议
实现 Ed25519 签名校验。

## P5-S03

### 步骤名称
Ed25519 签名校验

### 目标说明
防止未被信任发布方签名的 manifest 被 Client Agent 接受。

### 执行动作
- 新增 `ed25519-dalek` 依赖。
- 新增 `src/script/signature.rs`。
- 签名 payload 固定覆盖 schema、script_id、version、entry、sha256、permissions。
- 验证失败时拒绝进入 Lua VM。

### 主要产物
- `实现模块/client-agent/src/script/signature.rs`
- `Cargo.toml`
- `Cargo.lock`

### 编译检查
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- `secure_bootstrap_rejects_signature_mismatch` 通过。

### 残留清理
- 只提交公钥和签名，不提交私钥。

### 文档更新
- 已更新：`技术设计/脚本管理与安全机制设计.md`
- 已更新：`计划报告/风险与问题记录.md`

### 下一步建议
实现 SHA-256 完整性校验。

## P5-S04

### 步骤名称
SHA-256 完整性校验

### 目标说明
防止 Lua 文件内容被修改后仍继续执行。

### 执行动作
- 新增 `sha2` 依赖。
- 新增 `src/script/hash.rs`。
- 加载 Lua 文件后计算 SHA-256。
- 与 manifest 中的 `sha256` 做精确匹配。

### 主要产物
- `实现模块/client-agent/src/script/hash.rs`
- `实现模块/client-agent/src/script/mod.rs`

### 编译检查
- `cargo test -p client-agent` 通过。

### 调试/烟测
- `secure_bootstrap_rejects_hash_mismatch` 通过。

### 残留清理
- 未生成临时脚本文件。

### 文档更新
- 已更新：`测试验证/README.md`

### 下一步建议
实现 Lua 权限白名单。

## P5-S05

### 步骤名称
Lua 权限白名单

### 目标说明
让脚本只能使用 manifest 声明且本机配置允许的能力。

### 执行动作
- 新增 `src/script/permissions.rs`。
- 定义 `host.log`、`config.read`、`dm.access`。
- `ScriptSource` 携带权限集合。
- `lua_host` 按权限注册 `log/get_config/dm`。
- 未授权 API 不注册到 Lua globals。

### 主要产物
- `实现模块/client-agent/src/script/permissions.rs`
- `实现模块/client-agent/src/lua_host.rs`

### 编译检查
- `cargo clippy --workspace -- -D warnings` 通过。

### 调试/烟测
- `secure_bootstrap_rejects_permission_outside_whitelist` 通过。
- `lua_bootstrap_denies_unlisted_config_api` 通过。

### 残留清理
- 未留下占位权限。

### 文档更新
- 已更新：`技术设计/脚本管理与安全机制设计.md`

### 下一步建议
执行完整安全测试与烟测。

## P5-S06

### 步骤名称
安全测试与烟测

### 目标说明
验证脚本安全门不会破坏默认启动，同时能拒绝错误 hash、错误签名和越权权限。

### 执行动作
- 执行格式化。
- 执行 clippy。
- 执行 workspace 测试。
- 执行默认 Client Agent 启动烟测。

### 主要产物
- P5 安全测试结果。
- Client 默认启动烟测结果。

### 编译检查
- `cargo fmt --all` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- `cargo run -p client-agent` 通过。
- 输出 `current_script = bootstrap`。

### 残留清理
- `target/` 未提交。

### 文档更新
- 已更新：`测试验证/README.md`

### 下一步建议
整理 P5 文档。

## P5-S07

### 步骤名称
P5 文档与测试整理

### 目标说明
同步 P5 的阶段状态、测试记录、风险记录、模块说明和当前进度。

### 执行动作
- 更新 P5 阶段表。
- 更新项目总阶段预览表。
- 更新项目进度。
- 更新测试验证记录。
- 更新 Client Agent README。
- 记录私钥管理和 manifest 同步风险。

### 主要产物
- `计划报告/阶段P5-步骤表.md`
- `计划报告/项目进度.md`
- `测试验证/README.md`
- `实现模块/client-agent/README.md`
- `计划报告/风险与问题记录.md`

### 编译检查
- 文档步骤前置验证已通过。

### 调试/烟测
- P5 安全测试与默认启动烟测已通过。

### 残留清理
- 未生成临时文档草稿。

### 文档更新
- 已更新双 MD。

### 下一步建议
提交并推送 P5 可用版本。

## P5-S08

### 步骤名称
P5 阶段收口

### 目标说明
完成 P5 的版本备份和远端同步。

### 执行动作
- 检查 git 变更范围。
- 执行最终前后端构建、clippy、测试检查。
- 提交 P5 代码和文档。
- 推送 `main` 到 GitHub。

### 主要产物
- P5 脚本安全门提交。

### 编译检查
- `npm run build` 通过。
- `cargo fmt --all` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- P5 安全测试与默认启动烟测已通过。
- P5 Server 上报闭环烟测通过。
- `GET /api/client/status/local-dev-client` 返回 `current_script = bootstrap`、`online = true`。

### 残留清理
- `target/` 未提交。
- `node_modules/` 未提交。
- `dist/` 未提交。
- `target/p5-smoke` 已删除。

### 文档更新
- 已更新：`计划报告/步骤执行记录.md`

### 下一步建议
进入 P6 前，先确认发布归档与交接边界。

## P6-S01

### 步骤名称
发布边界确认

### 目标说明
固定 P6 只做发布归档、交接资料、部署说明、安全边界和最终验证，不扩展 Web 数据分析或客户端设置。

### 执行动作
- 核对当前 git 状态、远端仓库、阶段表和发布归档目录。
- 明确 `document-release` PR 流程不完全适用于当前主线收口，改用文档审计原则执行。
- 新增 P6 阶段步骤表。

### 主要产物
- `计划报告/阶段P6-步骤表.md`

### 编译检查
- 本步骤为文档边界确认，不涉及编译。

### 调试/烟测
- 已确认工作区起始状态干净。

### 残留清理
- 未生成临时文件。

### 文档更新
- 已更新：`计划报告/阶段P6-步骤表.md`

### 下一步建议
整理发布归档目录。

## P6-S02

### 步骤名称
发布归档目录整理

### 目标说明
让 `发布归档/` 成为 v1.0.0 交付入口，避免发布资料散落。

### 执行动作
- 更新发布归档 README。
- 建立发布资料索引。
- 明确不归档大漠二进制、授权资料和签名私钥。

### 主要产物
- `发布归档/README.md`

### 编译检查
- 文档结构检查通过。

### 调试/烟测
- 发布资料可从 README 直接定位。

### 残留清理
- 未生成临时草稿。

### 文档更新
- 已更新：`发布归档/README.md`

### 下一步建议
编写 v1.0.0 发布说明。

## P6-S03

### 步骤名称
v1.0.0 发布说明

### 目标说明
说明当前版本已包含和未包含的能力，避免把源码归档误解为生产部署版本。

### 执行动作
- 新增 v1.0.0 发布说明。
- 列出 Client、DmBridge、Server、Web Admin、脚本安全门和发布资料范围。
- 明确生产鉴权、TLS、数据分析、远程脚本下发等能力未纳入本版。

### 主要产物
- `发布归档/v1.0.0-发布说明.md`

### 编译检查
- 文档结构检查通过。

### 调试/烟测
- 发布范围和未包含范围表述清楚。

### 残留清理
- 未生成临时文件。

### 文档更新
- 已更新：`发布归档/v1.0.0-发布说明.md`

### 下一步建议
补交接清单。

## P6-S04

### 步骤名称
项目交接清单

### 目标说明
沉淀后续维护和继续开发前必须阅读的模块、文档和约束。

### 执行动作
- 新增交接清单。
- 按模块列出路径、当前状态和交接重点。
- 写入继续开发前检查顺序。

### 主要产物
- `发布归档/交接清单.md`

### 编译检查
- 文档结构检查通过。

### 调试/烟测
- 交接入口覆盖计划、设计、实现、测试和发布目录。

### 残留清理
- 未生成临时文件。

### 文档更新
- 已更新：`发布归档/交接清单.md`

### 下一步建议
补部署与运行指南。

## P6-S05

### 步骤名称
部署与运行指南

### 目标说明
记录 v1.0.0 本地构建、运行、联调和 DmBridge 编译方式。

### 执行动作
- 新增部署与运行指南。
- 记录 Rust、Node、Delphi、大漠插件环境要求。
- 记录 Server、Client、Web Admin 和 DmBridge 的运行命令。
- 修正 Client Server 环境变量名为 `CLIENT_AGENT_SERVER_HOST` 和 `CLIENT_AGENT_SERVER_PORT`。

### 主要产物
- `发布归档/部署与运行指南.md`

### 编译检查
- 文档命令经最终烟测核对。

### 调试/烟测
- 已用正确环境变量完成 Server/Client 上报闭环。

### 残留清理
- 未保留错误命令。

### 文档更新
- 已更新：`发布归档/部署与运行指南.md`

### 下一步建议
补安全边界说明。

## P6-S06

### 步骤名称
安全边界说明

### 目标说明
明确 v1.0.0 已有安全能力、未解决生产安全项和禁止入库资料。

### 执行动作
- 新增安全边界与私有资料说明。
- 列出禁止提交的密钥、授权、大漠资料、真实脚本和构建产物。
- 明确鉴权、TLS、CORS、脚本下发、私钥管理、数据库持久化未纳入 v1.0.0。

### 主要产物
- `发布归档/安全边界与私有资料说明.md`

### 编译检查
- 文档结构检查通过。

### 调试/烟测
- 风险边界已同步到风险记录。

### 残留清理
- 未新增敏感资料。

### 文档更新
- 已更新：`发布归档/安全边界与私有资料说明.md`
- 已更新：`计划报告/风险与问题记录.md`

### 下一步建议
执行最终验证。

## P6-S07

### 步骤名称
最终验证报告

### 目标说明
验证 v1.0.0 发布交接版本在本机可构建、可测试、可运行。

### 执行动作
- 执行 Web Admin 生产构建。
- 执行 Rust 格式化检查、clippy 和 workspace 测试。
- 执行 Client 默认启动烟测。
- 执行 Server/Client 本地上报烟测。
- 执行 DmBridge Win32 编译和导出符号检查。
- 清理 `dist/` 和 `target/p6-smoke/`。

### 主要产物
- `发布归档/最终验证报告.md`
- `测试验证/README.md`

### 编译检查
- `npm run build` 通过。
- `cargo fmt --all --check` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。
- `.\实现模块\dm-bridge\build.ps1` 通过。

### 调试/烟测
- `cargo run -p client-agent` 通过。
- P6 Server/Client 上报烟测通过，`current_script = bootstrap`。
- `tdump -ee target\dm-bridge\Win32\DmBridge.dll` 可看到 `dm_bridge_*` 导出函数。

### 残留清理
- 已删除 `实现模块/web-admin/dist/`。
- 已删除 `target/p6-smoke/`。
- `target/` 和 `node_modules/` 继续由 `.gitignore` 排除。

### 文档更新
- 已更新：`发布归档/最终验证报告.md`
- 已更新：`测试验证/README.md`

### 下一步建议
同步阶段状态、版本和变更记录。

## P6-S08

### 步骤名称
P6 版本收口

### 目标说明
完成 v1.0.0 版本元数据、阶段状态、进度、风险、变更和 GitHub 备份准备。

### 执行动作
- 更新根 `VERSION` 为 `v1.0.0`。
- 同步 `web-admin` private package 版本为 `1.0.0`。
- 更新项目总阶段预览表。
- 更新项目进度。
- 更新变更记录。
- 检查并准备提交推送。

### 主要产物
- `VERSION`
- `实现模块/web-admin/package.json`
- `实现模块/web-admin/package-lock.json`
- `计划报告/项目总阶段预览表.md`
- `计划报告/项目进度.md`
- `计划报告/变更记录.md`

### 编译检查
- P6-S07 最终验证已通过。

### 调试/烟测
- P6-S07 Server/Client、Client 默认启动和 DmBridge 导出符号烟测已通过。

### 残留清理
- 提交前将再次检查 git 状态和 ignored 构建产物。

### 文档更新
- 已更新双 MD。

### 下一步建议
提交并推送 P6 可交接版本到 GitHub。

## P7-S01

### 步骤名称
扩展边界设计

### 目标说明
固定 P7 只做真实状态详情、快照分析和 Web Admin 本地设置，不做历史数据库、远程配置下发和伪造指标。

### 执行动作
- 检查 Web Admin、Management Server 和 shared-types 当前结构。
- 确认当前协议只有 `client_id`、`online`、`current_script` 和时间戳，无法支撑真实详情。
- 新增 P7 阶段步骤表。
- 新增 Web 管理端信息扩展设计。

### 主要产物
- `计划报告/阶段P7-步骤表.md`
- `技术设计/Web管理端信息扩展设计.md`

### 编译检查
- 本步骤为设计边界确认，不涉及编译。

### 调试/烟测
- 已确认 P7 必须先扩展协议，再扩展 Web 展示。

### 残留清理
- 未生成临时文件。

### 文档更新
- 已更新：`计划报告/阶段P7-步骤表.md`
- 已更新：`技术设计/Web管理端信息扩展设计.md`
- 已更新：`技术设计/README.md`

### 下一步建议
扩展共享状态协议。

## P7-S02

### 步骤名称
状态协议扩展

### 目标说明
让 Client 状态协议能够承载真实运行详情、脚本摘要和 Server 上报摘要。

### 执行动作
- 扩展 `ClientStatus`。
- 新增 `ClientRuntimeInfo`。
- 新增 `ClientScriptInfo`。
- 新增 `ClientServerInfo`。
- 更新 shared-types 单元测试。
- 新增 shared-types README。

### 主要产物
- `实现模块/shared-types/src/lib.rs`
- `实现模块/shared-types/README.md`

### 编译检查
- `cargo test --workspace` 通过。

### 调试/烟测
- shared-types 测试确认默认状态包含 runtime 和 server 默认摘要。

### 残留清理
- 未引入未使用字段。

### 文档更新
- 已更新：`实现模块/shared-types/README.md`

### 下一步建议
扩展 Client Agent 状态映射。

## P7-S03

### 步骤名称
Client 状态映射

### 目标说明
让 Client Agent 从真实配置和运行时生成扩展后的状态详情。

### 执行动作
- 更新 `client-agent/src/status.rs`。
- 从根目录 `VERSION` 读取发布版本。
- 上报系统、架构、进程 ID。
- 上报 bootstrap 名称、指令上限、安全门和允许权限。
- 上报 Server 是否开启以及目标地址。
- 增加状态映射单元测试。

### 主要产物
- `实现模块/client-agent/src/status.rs`

### 编译检查
- `cargo fmt --all --check` 通过。
- `cargo test --workspace` 通过，client-agent 增至 21 项测试。

### 调试/烟测
- `cargo run -p client-agent` 输出包含 `runtime`、`script`、`server`。

### 残留清理
- 状态摘要未输出签名私钥、真实账号、商业脚本或大漠授权资料。

### 文档更新
- 已更新：`实现模块/client-agent/README.md`

### 下一步建议
同步 Web Admin 协议类型与状态衍生指标。

## P7-S04

### 步骤名称
Web 类型与分析工具

### 目标说明
让 Web Admin TypeScript 类型与 Rust 状态协议对齐，并提供页面所需衍生指标。

### 执行动作
- 更新 `types/protocol.ts`，增加 runtime、script、server 类型。
- 新增完整时间和相对时间格式化工具。
- 更新 `useDashboardStatus.ts`，增加离线数量、安全门数量和选中 Agent 版本。

### 主要产物
- `实现模块/web-admin/src/types/protocol.ts`
- `实现模块/web-admin/src/composables/useDashboardStatus.ts`

### 编译检查
- `npm run build` 通过。

### 调试/烟测
- 前端 TypeScript 检查通过，协议字段能被组件读取。

### 残留清理
- 未保留重复类型定义。

### 文档更新
- 已更新：`实现模块/web-admin/README.md`

### 下一步建议
实现快照分析面板。

## P7-S05

### 步骤名称
快照分析面板

### 目标说明
基于 Server 当前内存最新状态展示在线比例、脚本分布、最新上报和脚本安全门统计。

### 执行动作
- 新增 `SnapshotAnalytics.vue`。
- 按当前 Client 列表聚合脚本分布。
- 展示在线比例进度条。
- 展示安全门开启数量和最新上报年龄。
- 在 `App.vue` 中接入分析面板。

### 主要产物
- `实现模块/web-admin/src/components/SnapshotAnalytics.vue`
- `实现模块/web-admin/src/App.vue`

### 编译检查
- `npm run build` 通过。

### 调试/烟测
- Playwright 桌面和移动视口均能看到 `快照分析`。

### 残留清理
- 未伪造历史趋势或假指标。

### 文档更新
- 已更新：`技术设计/Web管理端信息扩展设计.md`

### 下一步建议
增强客户端详情和本地设置面板。

## P7-S06

### 步骤名称
详情与设置面板

### 目标说明
展示选中 Client 的真实详情，并把 Web Admin 本地连接设置从“连接面板”收敛为明确的设置面板。

### 执行动作
- 删除旧 `ConnectionPanel.vue`。
- 新增 `ClientSettingsPanel.vue`。
- 增强 `ClientDetail.vue`，按基本状态、Agent 运行、脚本设置、Server 上报分组展示。
- 更新 `App.vue` 右侧布局。

### 主要产物
- `实现模块/web-admin/src/components/ClientSettingsPanel.vue`
- `实现模块/web-admin/src/components/ClientDetail.vue`
- `实现模块/web-admin/src/App.vue`

### 编译检查
- `npm run build` 通过。

### 调试/烟测
- 页面显示 `本地設定`、`状态详情`、`v1.1.0`、`host.log, config.read` 和 Server 上报目标。

### 残留清理
- 已删除未使用的旧连接面板组件。

### 文档更新
- 已更新：`实现模块/web-admin/README.md`

### 下一步建议
执行完整构建、烟测和浏览器验证。

## P7-S07

### 步骤名称
构建与浏览器烟测

### 目标说明
验证 P7 协议扩展和 Web UI 在本机真实联调中可用。

### 执行动作
- 执行 Rust 格式化检查。
- 执行 clippy。
- 执行 workspace 测试。
- 执行 workspace 普通二进制构建。
- 执行 Web Admin 生产构建。
- 启动临时 Server 和 Vite。
- 运行 Client Agent 上报状态。
- 用 Playwright fallback 检查桌面和移动视口。
- 使用 `view_image` 检查截图。

### 主要产物
- P7 验证结果。
- `测试验证/README.md`

### 编译检查
- `cargo fmt --all --check` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。
- `cargo build --workspace` 通过。
- `npm run build` 通过。

### 调试/烟测
- Server/Client 上报烟测通过，API 返回 `release_version = v1.1.0`、`security_enabled = true`。
- Playwright 桌面 1440x920 通过，无横向溢出。
- Playwright 移动 390x844 通过，无横向溢出。
- `view_image` 已检查桌面和移动截图。

### 残留清理
- 已停止临时 Server。
- 已停止 Vite 子进程。
- 已删除 `实现模块/web-admin/dist/`。
- 已删除 `target/p7-smoke/`。
- 已删除 `target/p7-browser/`。

### 文档更新
- 已更新：`测试验证/README.md`

### 下一步建议
同步 P7 发布说明、进度、风险和变更记录。

## P7-S08

### 步骤名称
P7 收口与备份

### 目标说明
完成 v1.1.0 版本元数据、P7 阶段文档、发布归档和 GitHub 备份准备。

### 执行动作
- 更新根 `VERSION` 为 `v1.1.0`。
- 更新 `web-admin` private package 版本为 `1.1.0`。
- 新增 `发布归档/v1.1.0-发布说明.md`。
- 更新项目总阶段预览表。
- 更新项目进度。
- 更新风险与变更记录。
- 更新模块 README。

### 主要产物
- `VERSION`
- `发布归档/v1.1.0-发布说明.md`
- `计划报告/项目进度.md`
- `计划报告/风险与问题记录.md`
- `计划报告/变更记录.md`

### 编译检查
- P7-S07 最终验证已通过。

### 调试/烟测
- P7-S07 浏览器和 API 烟测已通过。

### 残留清理
- 提交前将再次检查 git 状态、ignored 产物和敏感文件。

### 文档更新
- 已更新双 MD。

### 下一步建议
提交并推送 P7 可用版本到 GitHub。

## P8-S01

### 步骤名称
历史边界设计

### 目标说明
固定 P8 只做进程内短期历史状态和真实趋势分析，不做生产数据库和长期统计。

### 执行动作
- 检查 `management-server/src/state.rs` 当前只保存最新状态。
- 检查 Web Admin 当前只读取最新状态与快照分析。
- 新增 P8 阶段步骤表。
- 新增历史状态与趋势分析设计。

### 主要产物
- `计划报告/阶段P8-步骤表.md`
- `技术设计/历史状态与趋势分析设计.md`

### 编译检查
- 本步骤为设计边界确认，不涉及编译。

### 调试/烟测
- 已确认 P8 先采用每 Client 50 条进程内历史，不引入数据库。

### 残留清理
- 未生成临时文件。

### 文档更新
- 已更新：`计划报告/阶段P8-步骤表.md`
- 已更新：`技术设计/历史状态与趋势分析设计.md`
- 已更新：`技术设计/README.md`

### 下一步建议
新增历史响应模型。

## P8-S02

### 步骤名称
历史状态模型

### 目标说明
为历史状态 API 建立共享响应结构，保证 Rust Server 和 Web Admin 语义一致。

### 执行动作
- 新增 `ClientStatusHistory`。
- 记录 `client_id`、`limit`、`total` 和 `items`。
- 增加 shared-types 单元测试。

### 主要产物
- `实现模块/shared-types/src/lib.rs`

### 编译检查
- `cargo test --workspace` 通过，shared-types 增至 3 项测试。

### 调试/烟测
- `status_history_reports_total_from_items` 通过。

### 残留清理
- 未新增无用字段。

### 文档更新
- 已更新：`实现模块/shared-types/README.md`

### 下一步建议
实现 Server 历史队列。

## P8-S03

### 步骤名称
Server 历史存储

### 目标说明
让 Management Server 在保存最新状态的同时保存短期历史队列。

### 执行动作
- 在 `ServerState` 中新增 `histories`。
- 设置 `CLIENT_HISTORY_LIMIT = 50`。
- `save_status` 同时更新最新状态和历史队列。
- 超过上限时删除最早记录。
- 增加边界测试。

### 主要产物
- `实现模块/management-server/src/state.rs`

### 编译检查
- `cargo fmt --all --check` 通过。
- `cargo test --workspace` 通过。

### 调试/烟测
- `state_keeps_bounded_client_history` 通过，确认超过 50 条后保留最新 50 条。

### 残留清理
- 未引入数据库临时文件。

### 文档更新
- 已更新：`实现模块/management-server/README.md`

### 下一步建议
暴露历史查询 API。

## P8-S04

### 步骤名称
Server 历史 API

### 目标说明
让 Web Admin 能查询指定 Client 的历史状态样本。

### 执行动作
- 新增 `GET /api/client/history/{client_id}`。
- 返回 `ClientStatusHistory`。
- 增加 Axum handler 测试。

### 主要产物
- `实现模块/management-server/src/app.rs`

### 编译检查
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过，management-server 增至 8 项测试。

### 调试/烟测
- `status_history_returns_samples_for_client` 通过。

### 残留清理
- 未改动既有最新状态 API 语义。

### 文档更新
- 已更新：`实现模块/management-server/README.md`

### 下一步建议
接入 Web 历史数据流。

## P8-S05

### 步骤名称
Web 历史数据流

### 目标说明
让 Web Admin 在刷新最新状态后同步读取选中 Client 的历史记录。

### 执行动作
- TypeScript 增加 `ClientStatusHistory`。
- API 客户端增加 `fetchClientHistory`。
- `useDashboardStatus` 增加 `selectedHistory` 和 `historyLimit`。
- 刷新当前 Client 后读取历史记录。

### 主要产物
- `实现模块/web-admin/src/types/protocol.ts`
- `实现模块/web-admin/src/api/managementServer.ts`
- `实现模块/web-admin/src/composables/useDashboardStatus.ts`

### 编译检查
- `npm run build` 通过。

### 调试/烟测
- Playwright 页面可读取历史记录并显示 `3/50`。

### 残留清理
- 未补造历史样本。

### 文档更新
- 已更新：`实现模块/web-admin/README.md`

### 下一步建议
实现历史趋势面板。

## P8-S06

### 步骤名称
趋势分析组件

### 目标说明
基于真实历史样本展示趋势概览、趋势线和最近记录。

### 执行动作
- 新增 `HistoryTrendPanel.vue`。
- 展示样本数、在线样本、脚本切换、最近样本。
- 根据历史样本绘制 SVG 趋势线。
- 显示最近 6 条历史记录。
- 接入 `App.vue` 主内容区。

### 主要产物
- `实现模块/web-admin/src/components/HistoryTrendPanel.vue`
- `实现模块/web-admin/src/App.vue`

### 编译检查
- `npm run build` 通过。

### 调试/烟测
- Playwright 桌面和移动视口显示 `歷史趨勢`、`3/50`、`v1.2.0`。
- `view_image` 发现初版 SVG 选择器导致图标放大，已修复。

### 残留清理
- 已将 CSS 选择器收敛为 `.trend-box svg/line/polyline/circle`，不影响 lucide 图标。

### 文档更新
- 已更新：`测试验证/README.md`

### 下一步建议
执行完整构建与浏览器烟测。

## P8-S07

### 步骤名称
构建与浏览器烟测

### 目标说明
验证 P8 历史 API 和 Web 趋势面板在真实本地联调中可用。

### 执行动作
- 执行 Rust 格式化检查。
- 执行 clippy。
- 执行 workspace 测试。
- 执行 workspace 普通二进制构建。
- 执行 Web Admin 生产构建。
- 启动临时 Server。
- 连续运行 Client Agent 三次上报历史。
- 启动临时 Vite。
- 使用 Playwright fallback 检查桌面和移动视口。
- 使用 `view_image` 检查截图。

### 主要产物
- P8 验证结果。
- `测试验证/README.md`

### 编译检查
- `cargo fmt --all --check` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。
- `cargo build --workspace` 通过。
- `npm run build` 通过。

### 调试/烟测
- 历史 API 烟测通过，`history_total = 3`、`history_limit = 50`。
- Playwright 桌面 1440x920 通过，无横向溢出。
- Playwright 移动 390x844 通过，无横向溢出。
- `view_image` 已检查桌面和移动截图。

### 残留清理
- 已停止临时 Server。
- 已停止 Vite 子进程。
- 已删除 `实现模块/web-admin/dist/`。
- 已删除 `target/p8-smoke/`。
- 已删除 `target/p8-browser/`。

### 文档更新
- 已更新：`测试验证/README.md`

### 下一步建议
同步 P8 发布说明、进度、风险和变更记录。

## P8-S08

### 步骤名称
P8 收口与备份

### 目标说明
完成 v1.2.0 版本元数据、P8 阶段文档、发布归档和 GitHub 备份准备。

### 执行动作
- 更新根 `VERSION` 为 `v1.2.0`。
- 更新 `web-admin` private package 版本为 `1.2.0`。
- 新增 `发布归档/v1.2.0-发布说明.md`。
- 更新项目总阶段预览表。
- 更新项目进度。
- 更新风险与变更记录。
- 更新模块 README。

### 主要产物
- `VERSION`
- `发布归档/v1.2.0-发布说明.md`
- `计划报告/项目进度.md`
- `计划报告/风险与问题记录.md`
- `计划报告/变更记录.md`

### 编译检查
- P8-S07 最终验证已通过。

### 调试/烟测
- P8-S07 历史 API、浏览器和截图检查均已通过。

### 残留清理
- 提交前将再次检查 git 状态、ignored 产物和敏感文件。

### 文档更新
- 已更新双 MD。

### 下一步建议
提交并推送 P8 可用版本到 GitHub。

## P8-R01

### 步骤名称
发布首个 GitHub Release

### 目标说明
将已验证的 v1.2.0 状态发布到 GitHub Releases，建立远端 tag 和可追踪发布记录。

### 执行动作
- 检查本地工作区、远端 `main` 和 tag 状态。
- 确认 GitHub API 凭证可用，且不输出任何密钥内容。
- 创建并推送 annotated tag `v1.2.0`。
- 使用 `发布归档/v1.2.0-发布说明.md` 创建 GitHub Release。
- 补充发布归档索引、项目进度、阶段总表和变更记录。

### 主要产物
- GitHub Release：`https://github.com/cowmaxs66/Wow_Web/releases/tag/v1.2.0`
- Git tag：`v1.2.0`
- `发布归档/GitHub发布记录.md`

### 编译检查
- 本步骤未修改代码。
- P8-S07 已完成 `cargo fmt --all --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace`、`cargo build --workspace` 和 `npm run build`。

### 调试/烟测
- GitHub API 返回 Release ID `349544316`。
- Release URL 可定位到 `v1.2.0`。
- 远端 tag `v1.2.0` 已存在。

### 残留清理
- 未生成临时文件。
- 未上传大漠 DLL、CHM/CHW、授权文件、真实账号资料、签名私钥或商业脚本。

### 文档更新
- 已更新：`发布归档/README.md`
- 已更新：`发布归档/v1.2.0-发布说明.md`
- 已新增：`发布归档/GitHub发布记录.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/变更记录.md`
- 已更新：`计划报告/步骤执行记录.md`

### 下一步建议
进入 P9：生产持久化设计。

## P9-S01

### 步骤名称
持久化边界设计

### 目标说明
固定 P9 先做可恢复的 JSONL 持久化 MVP，并沉淀后续 SQLite/Postgres schema。

### 执行动作
- 新增生产持久化设计。
- 明确 `MANAGEMENT_SERVER_HISTORY_PATH` 为启用开关。
- 固定默认不配置时保持 P8 纯内存行为。

### 主要产物
- `技术设计/生产持久化设计.md`
- `计划报告/阶段P9-步骤表.md`

### 编译检查
文档步骤，不涉及编译。

### 调试/烟测
已确认 P9 不改变既有 API 路径和 Web Admin 调用方式。

### 残留清理
未生成临时文件。

### 文档更新
已更新技术设计和阶段步骤表。

### 下一步建议
扩展 Management Server 配置。

## P9-S02

### 步骤名称
Server 配置扩展

### 目标说明
让 Management Server 可通过环境变量启用或关闭历史持久化。

### 执行动作
- 扩展 `ServerConfig`，新增 `history_path`。
- 新增 `MANAGEMENT_SERVER_HISTORY_PATH` 解析。
- 增加空值禁用和非空启用测试。

### 主要产物
- `实现模块/management-server/src/config.rs`

### 编译检查
`cargo test --workspace` 通过。

### 调试/烟测
配置测试确认空值不启用持久化，非空路径启用持久化。

### 残留清理
未生成临时文件。

### 文档更新
已更新 management-server README 和部署指南。

### 下一步建议
实现 JSONL 持久化模块。

## P9-S03

### 步骤名称
JSONL 持久化模块

### 目标说明
提供状态历史的追加写入、启动读取和坏数据显式报错能力。

### 执行动作
- 新增 `persistence.rs`。
- 实现 `HistoryPersistence::open/load/append`。
- 追加写入一行一个 JSON 状态信封。
- 对坏 JSON 行返回带行号错误。

### 主要产物
- `实现模块/management-server/src/persistence.rs`

### 编译检查
`cargo fmt --all --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace` 通过。

### 调试/烟测
持久化模块测试通过：可追加读取 JSONL，坏 JSON 行能返回第 1 行错误。

### 残留清理
测试临时目录在测试结束后删除。

### 文档更新
已更新生产持久化设计。

### 下一步建议
接入 ServerState 启动回放。

## P9-S04

### 步骤名称
Server 状态回放

### 目标说明
让 Server 启动时从 JSONL 恢复最新状态和历史 API 数据。

### 执行动作
- 新增 `ServerState::with_persistence`。
- `save_status` 改为返回持久化结果。
- API 保存失败时返回 500。
- Server 启动时根据配置选择内存模式或持久化模式。

### 主要产物
- `实现模块/management-server/src/state.rs`
- `实现模块/management-server/src/app.rs`
- `实现模块/management-server/src/main.rs`
- `实现模块/management-server/src/error.rs`

### 编译检查
`cargo test --workspace` 通过，management-server 增至 13 项测试。

### 调试/烟测
状态回放测试通过：重建 `ServerState` 后仍能读取历史和最新状态。

### 残留清理
未保留测试 JSONL 文件。

### 文档更新
已更新 management-server README。

### 下一步建议
修正编译包脚本路径。

## P9-S05

### 步骤名称
编译包路径兼容

### 目标说明
让 `client-agent.exe` 在普通编译包根目录运行时能读取包内脚本和 manifest。

### 执行动作
- 修改脚本相对路径解析。
- 优先使用当前工作目录下的脚本文件。
- 找不到时回退到源码模块目录，保持开发期兼容。

### 主要产物
- `实现模块/client-agent/src/script/mod.rs`

### 编译检查
`cargo fmt --all --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace` 通过。

### 调试/烟测
`relative_script_path_is_resolved_from_module_root` 测试通过，包内烟测也验证了当前目录脚本可用。

### 残留清理
未生成临时文件。

### 文档更新
已更新 client-agent README。

### 下一步建议
执行 P9 构建和持久化烟测。

## P9-S06

### 步骤名称
构建与持久化烟测

### 目标说明
验证 P9 代码在真实 Server/Client 运行中可保存并恢复历史。

### 执行动作
- 执行 Rust 格式化检查、clippy、workspace 测试和 debug build。
- 执行 Web Admin 生产构建。
- 启动启用 JSONL 的 Server。
- 连续运行 Client Agent 两次上报。
- 停止并重启 Server。
- 查询历史 API 和最新状态 API。

### 主要产物
- `测试验证/README.md`

### 编译检查
- `cargo fmt --all --check` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。
- `cargo build --workspace` 通过。
- `npm run build` 通过。

### 调试/烟测
- 重启前 `history_total = 2`。
- 重启后 `history_total = 2`。
- JSONL 文件 2 行。
- 最新状态 `release_version = v1.3.0`、`current_script = bootstrap`。

### 残留清理
临时 Server/Client 进程已停止。

### 文档更新
已更新测试验证记录和项目进度。

### 下一步建议
生成普通编译包。

## P9-S07

### 步骤名称
普通编译包生成

### 目标说明
生成可上传 GitHub Release 的普通 Windows x64 编译包，并验证包内可运行。

### 执行动作
- 执行 `cargo build --workspace --release`。
- 生成 `WoW_Framework_v1.3.0_windows_x64.zip`。
- 包含 Server/Client exe、Web Admin dist、示例配置、脚本和说明文档。
- 从包根目录执行 Server/Client 包内烟测。
- 检查敏感文件和大漠二进制。

### 主要产物
- `target/release-package/WoW_Framework_v1.3.0_windows_x64.zip`

### 编译检查
`cargo build --workspace --release` 通过。

### 调试/烟测
- 包内 Server/Client 烟测通过，历史 API 返回 `history_total = 1`。
- 包内敏感文件检查通过，禁止文件数量为 0。

### 残留清理
发布包位于 ignored 的 `target/` 目录，上传后清理本地临时烟测目录。

### 文档更新
已更新 v1.3.0 发布说明。

### 下一步建议
提交、推送、打 tag，并创建 GitHub Release。

## P11-S08

### 步骤名称
GitHub Release 发布

### 目标说明
将 v1.5.0 已验证源码和普通 Windows 编译包发布到 GitHub，便于用户下载测试。

### 执行动作
- 提交 P11 源码、工具和文档变更。
- 推送 `main` 到 GitHub。
- 创建并推送 `v1.5.0` tag。
- 创建 GitHub Release。
- 上传 `WoW_Framework_v1.5.0_windows.zip`。
- 回写 Release URL、asset ID、大小和 SHA-256。

### 主要产物
- GitHub Release：`https://github.com/cowmaxs66/Wow_Web/releases/tag/v1.5.0`
- Release asset：`WoW_Framework_v1.5.0_windows.zip`

### 编译检查
沿用 P11-S06 和 P11-S07 的最终构建结果：
- `cargo fmt --all --check` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。
- `npm run build` 通过。
- release build 和 x86 Client build 通过。

### 调试/烟测
- 包内双 exe 烟测通过。
- Client monitor 日志包含 Server 消息 `final package message works`。
- 状态历史包含 `release_version = v1.5.0` 和 `arch = x86`。
- Asset SHA-256 为 `b58bd9202b1d54ccbec4f2c4170d9190cc33c48e32b9d3e5e565a5fb52cde541`。

### 残留清理
Release 上传完成后清理 `target/p11-*`、`target/release-package` 和 Web Admin `dist` 临时产物。

### 文档更新
已更新：
- `发布归档/GitHub发布记录.md`
- `发布归档/v1.5.0-发布说明.md`
- `计划报告/项目总阶段预览表.md`
- `计划报告/阶段P11-步骤表.md`
- `计划报告/项目进度.md`

### 下一步建议
进入 P12 前确认优先方向：持久托盘右键菜单、原生设置窗口、Windows Service 或 Web 数据分析。

## P12-S01

### 步骤名称
开机启动边界设计

### 目标说明
固定 P12 只做当前用户开机启动与本机设置命令，不引入 Windows Service 或完整托盘程序。

### 执行动作
- 新增 Client 本机设置与开机启动设计文档。
- 明确使用 HKCU Run 注册项。
- 明确 Web Admin 只生成命令，不直接修改本机系统。

### 主要产物
- `技术设计/客户端本机设置与开机启动设计.md`
- `计划报告/阶段P12-步骤表.md`

### 编译检查
文档步骤不涉及编译。

### 调试/烟测
文档结构检查通过。

### 残留清理
未产生临时文件。

### 文档更新
已更新技术设计 README、项目总阶段预览表和项目进度。

### 下一步建议
实现 Client startup 模块。

## P12-S02

### 步骤名称
Client startup 模块

### 目标说明
为 Client Agent 增加当前用户开机启动查询、启用和停用能力。

### 执行动作
- 新增 `client-agent/src/startup.rs`。
- 使用 `reg.exe` 读写 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`。
- 启动命令固定为当前 `client-agent.exe --monitor`。
- 增加启动命令格式和注册表输出解析测试。

### 主要产物
- `实现模块/client-agent/src/startup.rs`

### 编译检查
`cargo test -p client-agent` 通过。

### 调试/烟测
startup 单元测试通过。

### 残留清理
未写入真实开机启动项。

### 文档更新
已更新 Client Agent README 和风险记录。

### 下一步建议
接入 CLI 与工具脚本。

## P12-S03

### 步骤名称
CLI 与工具脚本入口

### 目标说明
让用户可通过 exe 或 `tools/start-client.ps1` 管理当前用户开机启动。

### 执行动作
- `cli.rs` 增加 `--startup-status`、`--enable-startup`、`--disable-startup`。
- `main.rs` 将启动项命令路由到 startup 模块。
- `tools/start-client.ps1` 增加 `-StartupStatus`、`-EnableStartup`、`-DisableStartup`。

### 主要产物
- `实现模块/client-agent/src/cli.rs`
- `实现模块/client-agent/src/main.rs`
- `tools/start-client.ps1`

### 编译检查
`cargo fmt --all --check` 和 `cargo test -p client-agent` 通过。

### 调试/烟测
CLI 解析测试通过。

### 残留清理
未产生临时文件。

### 文档更新
已更新工具脚本 README 和部署运行指南。

### 下一步建议
扩展 Web 首次设置向导。

## P12-S04

### 步骤名称
Web 向导开机启动命令

### 目标说明
在 Web Admin 首次设置向导中展示 Client 开机启动查询、启用和停用命令。

### 执行动作
- `useSetupWizard.ts` 新增 startup status / enable / disable 命令生成。
- `SetupWizardPanel.vue` 新增 Client 开机启动命令区域和复制按钮。
- Client 启动命令改为调用 `tools/start-client.ps1`，适配源码和发布包。

### 主要产物
- `实现模块/web-admin/src/composables/useSetupWizard.ts`
- `实现模块/web-admin/src/components/SetupWizardPanel.vue`

### 编译检查
`npm run build` 通过。

### 调试/烟测
TypeScript 构建通过，命令生成未破坏首次设置向导构建。

### 残留清理
`dist/` 为 Web 构建产物，发布前清理或用于打包内嵌。

### 文档更新
已更新 Web Admin README 和测试验证记录。

### 下一步建议
执行 P12 全量验证。

## P12-S05

### 步骤名称
构建与烟测

### 目标说明
验证 P12 Rust、Web、CLI、工具脚本和浏览器 UI 变更均可运行。

### 执行动作
- 执行 `cargo fmt --all --check`。
- 执行 `cargo clippy --workspace -- -D warnings`。
- 执行 `cargo test --workspace`。
- 执行 `npm run build`。
- 执行 `cargo run -p client-agent -- --startup-status`。
- 执行 `tools/start-client.ps1 -ClientArch x64 -StartupStatus`。
- 使用 Playwright 检查 Web Admin 桌面和移动视口。

### 主要产物
- `target/p12-web-smoke/desktop.png`
- `target/p12-web-smoke/mobile.png`

### 编译检查
Rust fmt、clippy、workspace test 和 Web build 均通过。

### 调试/烟测
- Client startup status 只读查询通过，未修改注册表。
- 工具脚本 startup status 只读查询通过。
- Web Admin 显示 `Client 開機啟動`、`-StartupStatus`、`-EnableStartup` 和 `-DisableStartup`。
- 桌面 1440x1000 与移动 390x920 视口均无横向溢出。

### 残留清理
Vite preview 进程已停止；截图目录位于 ignored 的 `target/p12-web-smoke`，提交前清理。

### 文档更新
已更新测试验证记录和 v1.6.0 发布说明草稿。

### 下一步建议
执行文档收口、清理临时文件并提交推送。

## P12-S06

### 步骤名称
v1.6.0 编译包生成

### 目标说明
生成包含 Client 开机启动设置能力的 v1.6.0 Windows 测试包。

### 执行动作
- 执行 Web Admin 生产构建，用于 Server 内嵌资源。
- 执行 x64 release build。
- 执行 x86 Client release build。
- 编译 Win32 DmBridge。
- 生成 `WoW_Framework_v1.6.0_windows.zip`。
- 从包根目录执行双 exe 包内烟测。
- 从包根目录执行 `client-agent.exe --startup-status` 只读烟测。

### 主要产物
- `target/release-package/WoW_Framework_v1.6.0_windows.zip`

### 编译检查
- `npm run build` 通过。
- `cargo build --workspace --release` 通过。
- `cargo build -p client-agent --release --target i686-pc-windows-msvc` 通过。
- `.\实现模块\dm-bridge\build.ps1` 通过。

### 调试/烟测
- 包内 `management-server.exe` 内嵌 Web 正常。
- 包内 x86 `client-agent.exe --monitor` 可上报 `v1.6.0`。
- 包内 Client 日志包含 `startup package message works`。
- 包内 `client-agent.exe --startup-status` 只读查询通过，未修改注册表。
- 编译包敏感文件检查通过。

### 残留清理
发布包和烟测目录位于 ignored 的 `target/`，上传后清理。

### 文档更新
已更新 v1.6.0 发布说明、项目进度、阶段 P12 步骤表和测试验证记录。

### 下一步建议
提交、推送、打 tag，并创建 GitHub Release。

## P9-S08

### 步骤名称
GitHub Release 发布

### 目标说明
将 v1.3.0 源码 tag 和普通编译包发布到 GitHub Releases。

### 执行动作
- 创建并推送 annotated tag `v1.3.0`。
- 创建 GitHub Release `v1.3.0 - 生产持久化 MVP`。
- 上传 `WoW_Framework_v1.3.0_windows_x64.zip`。
- 回写 Release ID、asset ID、下载地址和 SHA-256。

### 主要产物
- GitHub Release：`https://github.com/cowmaxs66/Wow_Web/releases/tag/v1.3.0`
- Release asset：`WoW_Framework_v1.3.0_windows_x64.zip`

### 编译检查
- P9-S06 和 P9-S07 已完成全部构建检查。

### 调试/烟测
- Release API 创建成功，Release ID 为 `349552553`。
- Asset 上传成功，asset ID 为 `468073738`。
- Asset SHA-256 为 `8e1bc61b70348db81d7fbb7741fc18cb8c1306a1f1ab3968d5c34464c1bd9813`。

### 残留清理
- 未上传大漠 DLL、RegDll、CHM/CHW、授权文件、真实账号资料、签名私钥、真实商业脚本和 JSONL 历史文件。
- 本地临时发布包位于 ignored 的 `target/` 目录，最终清理前仅用于上传校验。

### 文档更新
- 已更新：`发布归档/GitHub发布记录.md`
- 已更新：`发布归档/v1.3.0-发布说明.md`
- 已更新：`计划报告/阶段P9-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`

### 下一步建议
进入 P10：数据库持久化升级。

## P10-S01

### 步骤名称
一键运行边界设计

### 目标说明
固定 P10 只做本机一键运行与首次设置向导，不进入 Windows Service、托盘程序和远程配置下发。

### 执行动作
- 新增 `技术设计/一键运行与首次设置向导设计.md`。
- 新增 `计划报告/阶段P10-步骤表.md`。
- 明确 x64 核心模式与 x86 DM 模式分离。

### 主要产物
- `技术设计/一键运行与首次设置向导设计.md`
- `计划报告/阶段P10-步骤表.md`

### 编译检查
文档结构检查通过。

### 调试/烟测
阶段边界与现有 P9 架构兼容，不修改已发布 API 语义。

### 残留清理
未产生临时文件。

### 文档更新
已更新 P10 阶段表和技术设计索引。

### 下一步建议
进入 Server Web 静态托管实现。

## P10-S02

### 步骤名称
Server Web 托管

### 目标说明
让 Management Server 可选托管 Web Admin 编译产物，支撑一键运行后直接访问管理页面。

### 执行动作
- 新增 `MANAGEMENT_SERVER_WEB_DIR` 配置。
- 引入 `tower-http` 静态文件服务。
- 增加静态目录 fallback 测试。

### 主要产物
- `实现模块/management-server/src/config.rs`
- `实现模块/management-server/src/app.rs`
- `实现模块/management-server/src/main.rs`

### 编译检查
- `cargo fmt --all --check` 通过。
- `cargo test -p management-server` 通过。

### 调试/烟测
`MANAGEMENT_SERVER_WEB_DIR` 挂载 Web Admin dist 后，首页 HTTP 200，API 路由仍优先匹配。

### 残留清理
联调 Server 已停止。

### 文档更新
已更新 Management Server README 和部署运行指南。

### 下一步建议
进入首次设置向导。

## P10-S03

### 步骤名称
首次设置向导

### 目标说明
在 Web Admin 中提供首次运行设置入口，生成 Server 与 Client 启动命令并保存浏览器本地配置。

### 执行动作
- 新增 `useSetupWizard.ts` 管理向导状态和命令生成。
- 新增 `SetupWizardPanel.vue`。
- 接入 `App.vue`，并在完成设置后触发看板刷新。
- 对 PowerShell 命令值做单引号转义，避免复制命令被输入内容破坏。

### 主要产物
- `实现模块/web-admin/src/composables/useSetupWizard.ts`
- `实现模块/web-admin/src/components/SetupWizardPanel.vue`
- `实现模块/web-admin/src/App.vue`

### 编译检查
`npm run build` 通过。

### 调试/烟测
Playwright 验证首次设置向导可将端口切到 `18130`，点击 `套用並完成` 后自动刷新并显示 `local-dev-client`、`1/1`、`v1.4.0`。

### 残留清理
浏览器截图保存在 ignored 的 `target/p10-browser/`，发布前清理。

### 文档更新
已更新 Web Admin README 和测试验证记录。

### 下一步建议
进入一键脚本实现。

## P10-S04

### 步骤名称
一键运行脚本

### 目标说明
提供源码目录和发布包都能使用的一键入口，减少首次试运行步骤。

### 执行动作
- 新增 `tools/common.ps1`、`start-server.ps1`、`start-client.ps1`。
- 新增 `run-local.cmd`、`start-server.cmd`、`start-client.cmd`。
- `.cmd` 优先使用 `pwsh`，无 `pwsh` 时回退 Windows PowerShell。
- 源码目录执行时先构建对应 exe，发布包目录直接使用 `bin/`。

### 主要产物
- `tools/`

### 编译检查
脚本语法由实际烟测覆盖。

### 调试/烟测
`pwsh -File tools/start-client.ps1 -DisableReport` 通过，输出 `release_version = v1.4.0`。

### 残留清理
删除空的旧 `工具脚本/` 目录，保留 ASCII `tools/`。

### 文档更新
已新增 `tools/README.md` 并更新部署运行指南。

### 下一步建议
进入全量构建与浏览器烟测。

## P10-S05

### 步骤名称
构建与浏览器烟测

### 目标说明
验证 P10 改动不破坏 Rust workspace、Web Admin 构建和浏览器渲染。

### 执行动作
- 执行 Rust fmt、clippy、workspace test。
- 执行 Web Admin 生产构建。
- 启动 Server 托管 Web Admin dist。
- 执行 Client 上报。
- 使用 Playwright 验证桌面与移动视口。

### 主要产物
- `target/p10-browser/desktop-recheck.png`
- `target/p10-browser/mobile-recheck.png`
- `target/p10-browser/wizard-apply.png`

### 编译检查
- `cargo fmt --all --check` 通过。
- `cargo clippy --workspace -- -D warnings` 通过。
- `cargo test --workspace` 通过。
- `npm run build` 通过。

### 调试/烟测
- `GET /health = ok`。
- API 返回 `release_version = v1.4.0`。
- 首页 HTTP 200。
- 桌面和移动页面无横向溢出。
- 首次设置向导联动通过。

### 残留清理
临时 Server 已停止。

### 文档更新
已更新测试验证记录、风险记录和项目进度。

### 下一步建议
生成 v1.4.0 编译包。

## P10-S06

### 步骤名称
v1.4.0 编译包生成

### 目标说明
生成包含 x64 Server、x64 Client、x86 Client、Win32 DmBridge、Web Admin 和 tools 的 Windows 编译包。

### 执行动作
- 执行 `cargo build --workspace --release`。
- 执行 `cargo build -p client-agent --release --target i686-pc-windows-msvc`。
- 执行 `.\实现模块\dm-bridge\build.ps1`。
- 生成 `WoW_Framework_v1.4.0_windows.zip`。
- 执行包内 Server/Client 烟测和敏感文件扫描。

### 主要产物
- `target/release-package/WoW_Framework_v1.4.0_windows.zip`

### 编译检查
- x64 release build 通过。
- x86 Client release build 通过。
- DmBridge Win32 编译通过。

### 调试/烟测
- 包内脚本启动 Server 成功。
- x64 Client 上报成功，JSONL 写入 1 行。
- x86 Client 基础启动成功，输出 `arch = x86`、`release_version = v1.4.0`。
- 包内敏感文件检查通过。

### 残留清理
发布包与烟测目录位于 ignored 的 `target/`，上传后清理。

### 文档更新
已更新 v1.4.0 发布说明和测试验证记录。

### 下一步建议
提交、推送、打 tag，并创建 GitHub Release。

## P10-S07

### 步骤名称
发布前归档

### 目标说明
将 P10 的验证结果、运行说明、风险和变更回写到项目文档。

### 执行动作
- 更新 `发布归档/v1.4.0-发布说明.md`。
- 更新 `计划报告/项目总阶段预览表.md`、`项目进度.md`、`变更记录.md`、`风险与问题记录.md`。
- 更新模块 README 与部署运行指南。

### 主要产物
- P10 文档归档。

### 编译检查
文档结构检查通过。

### 调试/烟测
文档与实际验证结果一致。

### 残留清理
未引入额外临时文件。

### 文档更新
已完成发布前归档。

### 下一步建议
执行 P10-S08：GitHub Release 发布。

## P10-S08

### 步骤名称
GitHub Release 发布

### 目标说明
将 v1.4.0 源码 tag 和普通 Windows 编译包发布到 GitHub Releases。

### 执行动作
- 提交 P10 源码与文档。
- 推送 `main` 到 GitHub。
- 创建并推送 annotated tag `v1.4.0`。
- 创建 GitHub Release `v1.4.0 - 一键运行与首次设置向导`。
- 上传 `WoW_Framework_v1.4.0_windows.zip`。
- 回写 Release ID、asset ID、下载地址和 SHA-256。

### 主要产物
- GitHub Release：`https://github.com/cowmaxs66/Wow_Web/releases/tag/v1.4.0`
- Release asset：`WoW_Framework_v1.4.0_windows.zip`

### 编译检查
- P10-S05 和 P10-S06 已完成全部构建检查。

### 调试/烟测
- Release API 创建成功，Release ID 为 `349571607`。
- Asset 上传成功，asset ID 为 `468098541`。
- Asset SHA-256 为 `05ce14b511821514c438813e2e1fdf233bc13f9273f3330ad9a9f3ceb52356a4`。

### 残留清理
- 未上传大漠 DLL、RegDll、CHM/CHW、授权文件、真实账号资料、签名私钥、真实商业脚本和 JSONL 历史文件。
- 本地临时发布包位于 ignored 的 `target/` 目录，最终清理前仅用于发布校验。

### 文档更新
- 已更新：`发布归档/GitHub发布记录.md`
- 已更新：`发布归档/v1.4.0-发布说明.md`
- 已更新：`计划报告/阶段P10-步骤表.md`
- 已更新：`计划报告/项目总阶段预览表.md`
- 已更新：`计划报告/项目进度.md`
- 已更新：`计划报告/步骤执行记录.md`

### 下一步建议
确认 P11 范围：数据分析、客户端详细设置、配置落地或服务化运行。

## P11-S01

### 步骤名称
单 exe 与客户端监控边界设计

### 目标说明
固定 P11 只做 Server 单 exe、Client monitor、本地日志、Server 消息和通知气泡，不做完整托盘菜单。

### 执行动作
- 新增 `技术设计/单exe与客户端监控设计.md`。
- 新增 `计划报告/阶段P11-步骤表.md`。
- 明确完整托盘右键菜单、原生设置窗口、WebSocket 和消息确认后续阶段再做。

### 主要产物
- `技术设计/单exe与客户端监控设计.md`
- `计划报告/阶段P11-步骤表.md`

### 编译检查
文档结构检查通过。

### 调试/烟测
阶段边界与 P10 兼容，不改变已有状态上报接口。

### 残留清理
未产生临时文件。

### 文档更新
已更新技术设计索引和阶段表。

### 下一步建议
进入 Server Web Admin 内嵌。

## P11-S02

### 步骤名称
Server 内嵌 Web Admin

### 目标说明
让 `management-server.exe` 在 release 构建中内嵌 Web Admin，减少发布包外部目录依赖。

### 执行动作
- 新增 `management-server/build.rs`。
- 新增 `embedded_web.rs`。
- Router 在未配置 `MANAGEMENT_SERVER_WEB_DIR` 时使用内嵌资源。
- Server 增加 `--open-browser` 启动参数。

### 主要产物
- `实现模块/management-server/build.rs`
- `实现模块/management-server/src/embedded_web.rs`

### 编译检查
`cargo test -p management-server` 通过。

### 调试/烟测
不设置 `MANAGEMENT_SERVER_WEB_DIR` 时，`management-server.exe` 首页 HTTP 200。

### 残留清理
临时 Server 已停止。

### 文档更新
已更新 Management Server README 和部署运行指南。

### 下一步建议
进入 Server 消息 API。

## P11-S03

### 步骤名称
Server 消息 API

### 目标说明
提供 Server 向指定 Client 写入消息的最小 API。

### 执行动作
- 新增 `ClientMessageRequest`、`ClientMessage`、`ClientMessageList`。
- ServerState 增加每个 Client 最近 100 条内存消息队列。
- 新增 `POST/GET /api/client/messages/{client_id}`。

### 主要产物
- `实现模块/shared-types/src/lib.rs`
- `实现模块/management-server/src/state.rs`
- `实现模块/management-server/src/app.rs`

### 编译检查
`cargo test -p shared-types` 和 `cargo test -p management-server` 通过。

### 调试/烟测
消息创建和查询 API 单元测试通过。

### 残留清理
未产生临时运行文件。

### 文档更新
已更新测试验证和风险记录。

### 下一步建议
进入 Client monitor。

## P11-S04

### 步骤名称
Client monitor / setup / log / notify

### 目标说明
让 `client-agent.exe` 支持常驻监控、本地日志、配置入口、日志入口和通知气泡。

### 执行动作
- 新增 `agent.rs`、`cli.rs`、`monitor.rs`、`local_log.rs`、`notifier.rs`。
- 保留默认单次执行并输出 JSON。
- 新增 `--monitor`、`--setup`、`--open-log`、`--notify`。
- 相对路径增加 exe 所在目录解析。

### 主要产物
- `实现模块/client-agent/src/agent.rs`
- `实现模块/client-agent/src/monitor.rs`
- `实现模块/client-agent/src/local_log.rs`
- `实现模块/client-agent/src/notifier.rs`

### 编译检查
`cargo test -p client-agent` 通过。

### 调试/烟测
Client monitor 能上报状态、轮询 Server 消息，并写入本地日志。

### 残留清理
临时 monitor 进程已停止。

### 文档更新
已更新 Client Agent README 和部署运行指南。

### 下一步建议
进入 Web Admin 消息发送表单。

## P11-S05

### 步骤名称
Web Admin 发送消息表单

### 目标说明
让用户可在 Client 详情面板直接发送 Server 消息，便于测试 monitor 日志和通知。

### 执行动作
- Web 协议类型增加 ClientMessage。
- API 客户端增加 `sendClientMessage`。
- `ClientDetail.vue` 增加 Server 消息表单。

### 主要产物
- `实现模块/web-admin/src/types/protocol.ts`
- `实现模块/web-admin/src/api/managementServer.ts`
- `实现模块/web-admin/src/components/ClientDetail.vue`

### 编译检查
`npm run build` 通过。

### 调试/烟测
Playwright 桌面和移动视口均显示 Server 消息表单，无横向溢出。

### 残留清理
浏览器截图位于 ignored 的 `target/p11-browser`，发布后清理。

### 文档更新
已更新 Web Admin README 和测试验证记录。

### 下一步建议
进入完整联调烟测。

## P11-S06

### 步骤名称
构建与联调烟测

### 目标说明
验证 P11 的 Rust、Web、内嵌 Web、Client monitor 和浏览器 UI 全链路。

### 执行动作
- 执行 `npm run build`。
- 执行 `cargo fmt --all --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace`。
- 启动内嵌 Web Server。
- 启动 Client monitor。
- 创建 Server 消息并检查 Client 日志。
- 使用 Playwright 检查桌面和移动视口。

### 主要产物
- `target/p11-smoke/`
- `target/p11-browser/`

### 编译检查
Rust 和 Web 构建检查均通过。

### 调试/烟测
- API 返回 `release_version = v1.5.0`。
- Client monitor 日志包含 Server 消息。
- Web Admin 显示 `Server 消息` 表单，桌面/移动无横向溢出。

### 残留清理
临时 Server 和 Client 进程已停止。

### 文档更新
已更新测试验证记录。

### 下一步建议
生成 v1.5.0 编译包。

## P11-S07

### 步骤名称
v1.5.0 编译包生成

### 目标说明
生成以 `management-server.exe` 和 `client-agent.exe` 为主入口的 Windows 测试包。

### 执行动作
- 执行 x64 release build。
- 执行 x86 Client release build。
- 编译 Win32 DmBridge。
- 生成 `WoW_Framework_v1.5.0_windows.zip`。
- 从包根目录执行双 exe 包内烟测。

### 主要产物
- `target/release-package/WoW_Framework_v1.5.0_windows.zip`

### 编译检查
- `cargo build --workspace --release` 通过。
- `cargo build -p client-agent --release --target i686-pc-windows-msvc` 通过。
- `.\实现模块\dm-bridge\build.ps1` 通过。

### 调试/烟测
- 包内 `management-server.exe` 内嵌 Web 正常。
- 包内 x86 `client-agent.exe --monitor` 可上报 `v1.5.0`。
- 包内 Client 日志包含 `package message works`。
- 包内敏感文件检查通过。

### 残留清理
发布包和烟测目录位于 ignored 的 `target/`，上传后清理。

### 文档更新
已更新 v1.5.0 发布说明、项目进度和测试验证记录。

### 下一步建议
提交、推送、打 tag，并创建 GitHub Release。
