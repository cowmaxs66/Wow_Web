# 阶段P20-步骤表

## 阶段目标
修复 v1.13.0 Client 分包根目录 `client-agent.exe` 双击无效果问题，确保正式入口不依赖 `start-client.cmd`，并发布 v1.13.1 热修复包。

## 完成标准
- Client 分包根目录 `client-agent.exe` 直接启动后可创建托盘宿主和 monitor 进程。
- Client 可向本机 Server 上报 `online = true`、`release_version = v1.13.1`。
- 启动链路不再弹出黑色命令行窗口。
- Windows PowerShell 5.1 下生成脚本不会因 UTF-8 无 BOM 和中文字符串解析失败。
- 三类发布包重新生成、验证并发布到 GitHub Release。

## 步骤总表
| 步骤 | 名称 | 主要产物 | 当前状态 | 备注 |
|------|------|----------|----------|------|
| P20-S01 | 问题复现与根因定位 | 本地复现记录 | 已完成 | v1.13.0 双击入口无进程、无日志 |
| P20-S02 | PowerShell 脚本编码与 STA 修正 | `ps_script.rs`、托盘/设置/更新脚本写入修正 | 已完成 | UTF-8 BOM + `-STA` |
| P20-S03 | GUI launcher 隐藏启动收敛 | `wow-launcher`、Client 隐藏启动逻辑 | 已完成 | 只保留 `CREATE_NO_WINDOW` |
| P20-S04 | 发布脚本编码修正 | `tools/package-release.ps1`、`dm-bridge/build.ps1` | 已完成 | 兼容中文源码路径 |
| P20-S05 | 构建与真实直启验证 | v1.13.1 三类 zip | 已完成 | 根目录 `client-agent.exe` 直启上线 |
| P20-S06 | v1.13.1 发布 | GitHub Release | 已完成 | Release ID `350055233`，三类 asset 已上传 |
