# 阶段P38-步骤表

## 阶段目标
修正 Server 桌面控制台入口，发布包默认启动独立桌面程序 `WoW-Desktop.exe`，不再调用 Edge App 模式。

## 步骤总表
| 步骤 | 名称 | 主要产物 | 当前状态 | 备注 |
|------|------|----------|----------|------|
| P38-S01 | 桌面壳模块接入 | `实现模块/desktop-console` | 已完成 | WRY/WebView2 桌面窗口，支持 `--url` |
| P38-S02 | 托盘启动链路修正 | `management-server/src/tray.rs` | 已完成 | 默认启动 `WoW-Desktop.exe`，浏览器仅备用 |
| P38-S03 | 发布包集成 | `tools/package-release.ps1` | 已完成 | 完整包和 Server 分包携带 `WoW-Desktop.exe` |
| P38-S04 | 编译与单元验证 | Rust 测试结果 | 已完成 | `desktop-console` 与托盘测试通过 |
| P38-S05 | 发布打包与归档 | v1.30.0 三类包、发布说明 | 已完成 | 三类包已生成，Server/Client/Desktop smoke 通过 |

## 完成标准
- `cargo test -p desktop-console` 通过。
- `cargo test -p management-server tray` 通过。
- `tools/package-release.ps1` 成功生成 v1.30.0 三类 Windows 包。
- 完整包和 Server 分包根目录包含 `WoW-Desktop.exe`。
- 文档明确说明 WebView2 Runtime 与 Edge 浏览器窗口的区别。
