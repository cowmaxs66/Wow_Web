# 项目进度

## 当前阶段
P20 Client 正式直启热修复

## 当前目标
P20 进行中：已修复 Client 分包根目录 `client-agent.exe` 双击无效果问题，v1.13.1 三类编译包已生成并通过真实直启烟测，待 GitHub Release 发布收口。

## 已完成项
- 已确认原始 v0.1 方案的方向可行。
- 已将 MVP 收敛为单机 Client Agent 与共享协议类型优先。
- 已建立项目顶层目录职责。
- 已建立最小 Rust workspace 结构。
- 已通过 `cargo test --workspace` 编译与测试验证。
- 已通过 `cargo run -p client-agent` 状态消息烟测。
- 已通过 `cargo run -p management-server` 协议复用烟测。
- 已归档用户提供的原始 v0.1 设计文档。
- 已初始化本地 git，并完成首次提交 `c9a9c7e`。
- 已绑定 GitHub 远端 `https://github.com/cowmaxs66/Wow_Web.git`。
- 已推送 `main` 到 GitHub。
- 已将代码组织约束写入 `AGENTS.md`。
- 已完成 Client Agent TOML 配置读取。
- 已完成最小 Lua 宿主和白名单 `log/get_config` API。
- 已将 client-agent 代码拆分为入口、配置、Lua 宿主多个小文件。
- 已将 Lua bootstrap 从 TOML 内联脚本迁移到 `scripts/bootstrap.lua`。
- 已新增 Lua 指令上限，能够拦截无限循环脚本。
- 已新增 `src/status.rs`，将 Lua 执行报告转换为共享状态结构。
- 已新增 `src/logging.rs`，使用 `tracing` 管理本地结构化日志。
- 已通过 `cargo test --workspace` 验证 client-agent 4 项测试与 shared-types 1 项测试。
- 已通过 `cargo run -p client-agent` 验证脚本文件加载与状态 JSON 输出。
- 已完成 DmBridge C ABI 契约设计。
- 已建立 Delphi DmBridge 最小工程。
- 已通过 `C:\RAD13\bin\dcc32.exe` 编译 Win32 `DmBridge.dll`。
- 已用 `tdump` 确认 `dm_bridge_*` 导出函数存在。
- 已将 DmBridge 从直接 STA 模式升级为内部 STA Worker 队列。
- 已将 Worker 拆分为 `Worker.Types`、`Worker.Request`、`Worker.Thread` 和 `Worker` 门面单元。
- 已再次通过 `C:\RAD13\bin\dcc32.exe` 编译 Win32 `DmBridge.dll`。
- 已再次用 `tdump` 确认 `dm_bridge_*` 导出函数完整。
- 已修复 Delphi ABI 版本常量与导出函数同名导致的递归栈溢出。
- 已修复 Delphi `TThread.Start` 调用时机错误。
- 已修复大漠无参 COM 方法调用写法。
- 已新增 Rust `src/dm_bridge/`，通过 `libloading` 加载 DmBridge C ABI。
- 已新增 Lua `dm` 高层 API，Lua 不直接接触 C ABI 指针。
- 已通过 32 位 Rust 烟测读取 DmBridge ABI 版本。
- 已通过 32 位 Rust COM 烟测验证 `init -> ver -> get_color -> move_to -> shutdown`。
- 已通过 32 位 Lua COM 烟测验证 `dm.init -> dm.ver -> dm.get_color -> dm.move_to -> dm.shutdown`。
- 已建立 P3 Server 通讯闭环设计。
- 已新增 `HealthResponse` 和 `StatusAck` 共享响应类型。
- 已将 `management-server` 升级为 Axum HTTP 服务。
- 已实现 `GET /health`、`POST /api/client/status`、`GET /api/client/status/{client_id}`。
- 已新增 Client `server_reporter` 状态上报模块，默认关闭，可通过环境变量启用。
- 已将 `server_reporter` 拆分为上报入口、错误类型、响应解析三个职责文件。
- 已通过本地集成烟测：启动 Server、Client 上报、Server 查询 `local-dev-client` 状态。
- 已通过最终全量检查：`cargo fmt --all`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace`、P3 本地通讯烟测。
- 已进入 P4，固定 Web 管理端 MVP 只读取健康检查和 Client 状态 API。
- 已初始化 `实现模块/web-admin` Vue/Vite/TypeScript 工程。
- 已新增 Management Server `GET /api/client/status` 状态列表 API。
- 已为 P4 本地 Web Admin 联调增加开发期 CORS。
- 已实现 Web Admin API 客户端、状态看板、客户端列表、连接设置和状态详情。
- 已将 Web Admin 状态刷新逻辑拆入 `src/composables/useDashboardStatus.ts`，避免入口组件膨胀。
- 已通过 `npm run build` 前端生产构建。
- 已通过 P4 API 烟测：Server、Client、状态列表 API 三端闭环。
- 已通过 Playwright fallback 浏览器验证：桌面 1440x920、移动 390x844 均显示真实 `local-dev-client` 和 `bootstrap`，无横向溢出。
- 已完成 P4 最终检查，临时 Server、Vite、截图目录和 `dist/` 已清理。
- 已进入 P5，固定本阶段只做本机脚本安全门，不做远程下发和 Web 扩展。
- 已新增 `script_security` 配置，默认启用 manifest 安全校验。
- 已新增 `scripts/bootstrap.manifest.json`，记录 bootstrap 版本、入口、SHA-256、权限和 Ed25519 签名。
- 已新增脚本 manifest 读取、字段校验、Ed25519 签名校验、SHA-256 完整性校验。
- 已新增 Lua 权限白名单，未授权的 `log/get_config/dm` 不注册。
- 已通过 hash 不匹配、签名不匹配、权限越界、未授权 API 拒绝测试。
- 已通过默认 `cargo run -p client-agent` 安全启动烟测。
- 已通过 P5 Server 上报闭环烟测：安全门通过后仍可上报 `local-dev-client` 状态。
- 已通过 `npm run build`，确认 P5 后 Web Admin 构建未受影响。
- 已进入 P6，固定本阶段只做发布归档、交接和最终验证，不扩展 Web 数据分析或客户端设置。
- 已新增 `计划报告/阶段P6-步骤表.md`。
- 已更新 `发布归档/README.md`，建立 v1.0.0 发布资料索引。
- 已新增 `发布归档/v1.0.0-发布说明.md`。
- 已新增 `发布归档/交接清单.md`。
- 已新增 `发布归档/部署与运行指南.md`。
- 已新增 `发布归档/安全边界与私有资料说明.md`。
- 已新增 `发布归档/最终验证报告.md`。
- 已将根版本 `VERSION` 更新为 `v1.0.0`。
- 已将 `web-admin` private package 版本同步为 `1.0.0`。
- 已通过 P6 最终验证：`npm run build`、`cargo fmt --all --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace`、Client 默认启动、Server/Client 上报烟测、DmBridge Win32 编译和导出符号检查。
- 已发现并修正部署指南中错误的 Client Server 环境变量名。
- 已清理 `实现模块/web-admin/dist/` 和 `target/p6-smoke/` 临时产物。
- 已进入 P7，固定本阶段只做真实状态详情、快照分析和 Web Admin 本地设置，不做历史数据库和远程配置下发。
- 已新增 `技术设计/Web管理端信息扩展设计.md`。
- 已新增 `计划报告/阶段P7-步骤表.md`。
- 已扩展 `shared-types::ClientStatus`，增加 `runtime`、`script`、`server` 三类摘要。
- 已新增 `实现模块/shared-types/README.md`。
- 已扩展 `client-agent/src/status.rs`，从真实配置和运行时生成状态详情。
- 已扩展 Web Admin TypeScript 协议类型和状态 composable。
- 已新增 `SnapshotAnalytics.vue`，展示在线比例、脚本分布、最新上报和安全门统计。
- 已新增 `ClientSettingsPanel.vue`，替代原连接面板，明确设置只保存于浏览器本地。
- 已增强 `ClientDetail.vue`，展示 Agent 运行、脚本设置、Server 上报和协议 JSON。
- 已将根版本 `VERSION` 更新为 `v1.1.0`。
- 已将 `web-admin` private package 版本同步为 `1.1.0`。
- 已通过 P7 最终验证：`cargo fmt --all --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace`、`cargo build --workspace`、`npm run build`、Server/Client 上报烟测和 Playwright 桌面/移动浏览器烟测。
- 已发现并记录旧普通二进制混用风险：协议扩展后直接运行旧 `target/debug/client-agent.exe` 会得到旧字段，发布烟测前必须先执行 `cargo build --workspace`。
- 已清理 `实现模块/web-admin/dist/`、`target/p7-smoke/` 和 `target/p7-browser/` 临时产物。
- 已进入 P8，固定本阶段只做进程内短期历史和真实趋势分析，不做生产数据库。
- 已新增 `技术设计/历史状态与趋势分析设计.md`。
- 已新增 `计划报告/阶段P8-步骤表.md`。
- 已新增 `ClientStatusHistory` 共享响应结构。
- 已扩展 `management-server::ServerState`，每个 Client 保留最近 50 条状态历史。
- 已新增 `GET /api/client/history/{client_id}` 历史查询 API。
- 已扩展 Web Admin API 和状态 composable，读取选中 Client 历史。
- 已新增 `HistoryTrendPanel.vue`，展示样本数、在线样本、脚本切换、最近样本、趋势线和最近记录。
- 已将根版本 `VERSION` 更新为 `v1.2.0`。
- 已将 `web-admin` private package 版本同步为 `1.2.0`。
- 已通过 P8 最终验证：`cargo fmt --all --check`、`cargo clippy --workspace -- -D warnings`、`cargo test --workspace`、`cargo build --workspace`、`npm run build`、Server/Client 历史 API 烟测和 Playwright 桌面/移动浏览器烟测。
- 已发现并修复 `HistoryTrendPanel.vue` 中 `svg` 选择器过宽导致图标放大的 UI 问题。
- 已清理 `实现模块/web-admin/dist/`、`target/p8-smoke/` 和 `target/p8-browser/` 临时产物。
- 已发布首个 GitHub Release：`v1.2.0 - 历史状态与趋势分析`。
- 已推送远端 tag `v1.2.0`，指向已验证提交 `41ca63e6b29658fd572d83c4378b6980dbf0bd36`。
- 已进入 P9，固定本阶段先做 JSONL 持久化 MVP，不直接引入数据库驱动。
- 已新增 `技术设计/生产持久化设计.md`，记录 JSONL MVP、SQLite/Postgres schema、保留策略和备份恢复。
- 已新增 `计划报告/阶段P9-步骤表.md`。
- 已新增 Management Server `MANAGEMENT_SERVER_HISTORY_PATH` 配置。
- 已新增 `management-server/src/persistence.rs`，支持 JSONL 追加、启动读取和坏行报错。
- 已扩展 `ServerState::with_persistence`，Server 启动可回放历史并恢复最新状态。
- 已修正 Client Agent 打包后脚本路径解析，发布包根目录下的 `scripts/bootstrap.lua` 可被优先读取。
- 已将根版本 `VERSION` 更新为 `v1.3.0`。
- 已将 `web-admin` private package 版本同步为 `1.3.0`。
- 已通过 P9 最终验证：Rust fmt、clippy、workspace test、debug build、release build、Web Admin build、Server/Client JSONL 重启恢复烟测。
- 已生成普通编译包 `WoW_Framework_v1.3.0_windows_x64.zip`，并通过包内 Server/Client 烟测。
- 已确认普通编译包不包含大漠二进制、CHM/CHW、`.env` 和 JSONL 历史文件。
- 已发布 GitHub Release：`v1.3.0 - 生产持久化 MVP`。
- 已上传普通编译包 `WoW_Framework_v1.3.0_windows_x64.zip`。
- 已进入 P10，固定本阶段先做本机一键运行与首次设置向导，不做 Windows Service、托盘程序和远程配置下发。
- 已新增 `技术设计/一键运行与首次设置向导设计.md`。
- 已新增 `计划报告/阶段P10-步骤表.md`。
- 已新增 `tools/`，包含 `start-server.ps1`、`start-client.ps1`、`run-local.cmd` 和 README。
- 已扩展 Management Server `MANAGEMENT_SERVER_WEB_DIR` 配置，可直接托管 Web Admin 静态文件。
- 已新增 Web Admin 首次设置向导，可保存本地设置、生成 Server/Client 启动命令，并区分 x64 核心模式和 x86 DM 模式。
- 已修正源码模式脚本优先复用旧二进制的问题，源码目录执行时会先重新构建对应可执行文件。
- 已将根版本 `VERSION` 更新为 `v1.4.0`。
- 已将 `web-admin` private package 版本同步为 `1.4.0`。
- 已通过 P10 验证：Rust fmt、clippy、workspace test、Web Admin build、脚本烟测、Server/Web/API 联调、Playwright 桌面/移动浏览器烟测。
- 已通过首次设置向导联动验证：端口改为 `18130` 后自动刷新看板，页面显示 `local-dev-client`、`v1.4.0` 和在线状态。
- 已生成编译包 `WoW_Framework_v1.4.0_windows.zip`，包含 x64 Server、x64 Client、x86 Client、Win32 DmBridge、Web Admin、tools、示例配置和公开文档。
- 已通过包内烟测：包内脚本启动 Server，x64 Client 上报成功，x86 Client 可基础启动并输出 `arch = x86`。
- 已确认编译包不包含 `dm.dll`、`RegDll.dll`、CHM/CHW、授权文件、`.env`、JSONL 历史文件和私有资料。
- 已发布 GitHub Release：`v1.4.0 - 一键运行与首次设置向导`。
- 已上传普通编译包 `WoW_Framework_v1.4.0_windows.zip`。
- 已进入 P11，固定本阶段只做 Server 单 exe、Client monitor、本地日志、Server 消息和通知气泡，不做完整托盘右键菜单。
- 已新增 `技术设计/单exe与客户端监控设计.md`。
- 已新增 `计划报告/阶段P11-步骤表.md`。
- 已新增 Management Server 编译期 Web Admin 内嵌资源机制。
- 已新增 Server 消息 API：`POST/GET /api/client/messages/{client_id}`。
- 已新增 Web Admin 状态详情中的 Server 消息发送表单。
- 已将 Client Agent 入口拆分为 `run once`、`--monitor`、`--setup`、`--open-log`、`--notify` 多种模式。
- 已新增 Client 本地日志：`logs/client-agent.log` 和 `logs/status-history.jsonl`。
- 已新增 Client monitor 轮询 Server 消息并弹出 Windows 通知气泡。
- 已将根版本 `VERSION` 更新为 `v1.5.0`。
- 已将 `web-admin` private package 版本同步为 `1.5.0`。
- 已通过 P11 验证：Rust fmt、clippy、workspace test、Web Admin build、Server 内嵌 Web 烟测、Client monitor 消息日志烟测、Playwright 桌面/移动浏览器烟测。
- 已生成编译包 `WoW_Framework_v1.5.0_windows.zip`。
- 已通过包内双 exe 烟测：`management-server.exe` 内嵌 Web 正常，x86 `client-agent.exe --monitor` 可上报 `v1.5.0` 并记录 Server 消息。
- 已发布 GitHub Release：`v1.5.0 - 单 exe 与客户端监控基础`。
- 已上传编译包 `WoW_Framework_v1.5.0_windows.zip`，SHA-256 为 `b58bd9202b1d54ccbec4f2c4170d9190cc33c48e32b9d3e5e565a5fb52cde541`。
- 已进入 P12，固定本阶段只做当前用户开机启动与本机设置命令，不做 Windows Service。
- 已新增 `技术设计/客户端本机设置与开机启动设计.md`。
- 已新增 `计划报告/阶段P12-步骤表.md`。
- 已新增 Client startup 模块，使用 HKCU Run 管理当前用户开机启动。
- 已新增 `--startup-status`、`--enable-startup`、`--disable-startup`。
- 已扩展 `tools/start-client.ps1`，提供 `-StartupStatus`、`-EnableStartup`、`-DisableStartup`。
- 已扩展 Web Admin 首次设置向导，可生成 Client 开机启动查询、启用和停用命令。
- 已将根版本 `VERSION` 更新为 `v1.6.0`。
- 已将 `web-admin` private package 版本同步为 `1.6.0`。
- 已通过 P12 验证：Rust fmt、clippy、workspace test、Web Admin build、startup status 只读烟测和 Playwright 桌面/移动浏览器烟测。
- 已生成编译包 `WoW_Framework_v1.6.0_windows.zip`。
- 已通过包内双 exe 烟测：`management-server.exe` 内嵌 Web 正常，x86 `client-agent.exe --monitor` 可上报 `v1.6.0` 并记录 Server 消息。
- 已通过包内 `client-agent.exe --startup-status` 只读烟测，未修改注册表。
- 已发布 GitHub Release：`v1.6.0 - Client 本机设置与开机启动`。
- 已上传编译包 `WoW_Framework_v1.6.0_windows.zip`，SHA-256 为 `88ee7c8d8cea5f3453b2e2976b5aff21681288e1548086ab639097d33d5d64e7`。
- 已进入 P13，固定本阶段做正式运行基础，不再把测试命令作为主要使用方式。
- 已新增 `技术设计/正式运行基础设计.md`。
- 已新增 `计划报告/阶段P13-步骤表.md`。
- 已新增 Windows Service 入口与管理命令：`--service-run/install/uninstall/start/stop/status`。
- 已新增托盘常驻入口：`--tray`，右键菜单包含 monitor、设置、日志、开机启动、Service 和更新操作。
- 已新增原生设置窗口：`--settings-window`。
- 已新增自动更新检查/下载：`--update-check`、`--update-download`。
- 已新增 `ClientCommand` 共享协议、Server 命令队列 API 和 Client monitor 命令轮询执行。
- 已扩展 Web Admin Client 详情，支持下发白名单本机操作。
- 已扩展 `tools/start-client.ps1` 和 `tools/run-local.cmd`，一键运行默认进入托盘形态。
- 已将根版本 `VERSION` 更新为 `v1.7.0`。
- 已将 `web-admin` private package 版本同步为 `1.7.0`。
- 已通过 P13 验证：Rust fmt、clippy、workspace test、Web Admin build、Service 状态查询、更新检查、远程命令下发烟测、In-app Browser 桌面/移动烟测和包内烟测。
- 已生成编译包 `WoW_Framework_v1.7.0_windows.zip`。
- 已通过包内烟测：`management-server.exe` 内嵌 Web HTTP 200，x86 `client-agent.exe --monitor` 可上报 `v1.7.0` 并执行远程命令。
- 已发布 GitHub Release：`v1.7.0 - 正式运行基础`。
- 已上传编译包 `WoW_Framework_v1.7.0_windows.zip`，SHA-256 为 `274eae499f064542490056900b086d12bb38be38ce32e7f98f8d454b1c5bc8cb`。
- 已进入 P14，固定本阶段做双击正式入口，不再把命令参数作为普通用户主入口。
- 已新增 `技术设计/双击正式入口设计.md`。
- 已新增 `计划报告/阶段P14-步骤表.md`。
- 已将 `client-agent.exe` 无参数默认行为改为托盘常驻入口。
- 已新增 `client-agent.exe --run-once` 保留旧的单次 JSON 维护入口。
- 已将 `management-server.exe` 无参数默认行为改为启动 Server 并打开 Web 管理页。
- 已新增 `management-server.exe --no-open-browser` / `--api-only` 保留自动化入口。
- 已将开机启动命令从 `client-agent.exe --monitor` 调整为 `client-agent.exe`。
- 已将根版本 `VERSION` 更新为 `v1.8.0`。
- 已将 `web-admin` private package 版本同步为 `1.8.0`。
- 已通过 P14 验证：Rust fmt、clippy、workspace test、Web Admin build、Client `--run-once`、Server `--no-open-browser` 和包内烟测。
- 已生成编译包 `WoW_Framework_v1.8.0_windows.zip`。
- 已发布 GitHub Release：`v1.8.0 - 双击正式入口`。
- 已上传编译包 `WoW_Framework_v1.8.0_windows.zip`，SHA-256 为 `1b7e7e237a5edaf47673fefc1d10a75a4311790b082bf9dae83de4d39d944202`。
- 已进入 P15，固定本阶段做无控制台正式入口、当前用户安装器和正式打包脚本。
- 已新增 `技术设计/无控制台正式入口与安装器设计.md`。
- 已新增 `计划报告/阶段P15-步骤表.md`。
- 已新增 `实现模块/wow-launcher`，提供 Server、Client、安装和卸载 GUI launcher。
- 已将发布包根目录 `management-server.exe`、`client-agent.exe` 调整为 GUI launcher。
- 已将核心维护 exe 调整为 `bin/management-server-core.exe`、`bin/client-agent-core.exe`、`bin/client-agent-x64-core.exe`。
- 已新增当前用户安装脚本和卸载脚本，安装到 `%LOCALAPPDATA%\WoWFramework` 并创建快捷方式。
- 已新增 `tools/package-release.ps1`，固化正式发布包结构。
- 已将开机启动逻辑调整为：core 运行时优先写入根目录 `client-agent.exe` launcher。
- 已将根版本 `VERSION` 更新为 `v1.9.0`。
- 已将 `web-admin` private package 版本同步为 `1.9.0`。
- 已通过 P15 验证：Rust fmt、clippy、workspace test、Web Admin build、打包脚本、PE 子系统检查、Client/Server core 包内烟测、安装脚本语法检查和 zip 敏感文件检查。
- 已生成编译包 `WoW_Framework_v1.9.0_windows.zip`，SHA-256 为 `c3186bb7d1efc3954e7401581398ce306cc1cecaf919a8dac233fb81ff0705ca`。
- 已发布 GitHub Release：`v1.9.0 - 无控制台正式入口与安装器`。
- 已上传编译包 `WoW_Framework_v1.9.0_windows.zip`，asset ID 为 `468719146`。
- 已进入 P16，固定本阶段做自动更新自替换，不做 MSI/MSIX 和发布包签名验证。
- 已新增 `技术设计/自动更新自替换设计.md`。
- 已新增 `计划报告/阶段P16-步骤表.md`。
- 已新增 `client-agent-core.exe --update-apply`。
- 已新增独立 updater 脚本生成逻辑，更新时保留 `data/logs/updates`。
- 已在托盘菜单增加“安装更新并退出”。
- 已扩展 `tools/start-client.ps1 -UpdateApply`。
- 已将根版本 `VERSION` 更新为 `v1.10.0`。
- 已将 `web-admin` private package 版本同步为 `1.10.0`。
- 已通过 P16 验证：Rust fmt、clippy、workspace test、Web Admin build、打包脚本、Client/Server core 包内烟测、`--update-apply` 防降级烟测、PE 子系统和 zip 敏感文件检查。
- 已生成编译包 `WoW_Framework_v1.10.0_windows.zip`，SHA-256 为 `72ecf47084a7290047f55c5cd9fffeae6621ef42fe83a4f112ef5b6231194038`。
- 已发布 GitHub Release：`v1.10.0 - 自动更新自替换`。
- 已上传编译包 `WoW_Framework_v1.10.0_windows.zip`，asset ID 为 `468730945`。
- 已进入 P17，固定本阶段补 Server 远程更新命令、Web Admin 左侧导航功能页和 Client 在线状态收敛。
- 已新增 `技术设计/服务端远程更新与导航补全设计.md`。
- 已新增 `计划报告/阶段P17-步骤表.md`。
- 已新增 Server 命令白名单 `update.apply`。
- 已新增 Client 远程命令执行分支 `update.apply`，复用 `updater::apply_update()`。
- 已将 Server 最新状态查询改为按 60 秒心跳窗口收敛在线状态。
- 已将 Web Admin 左侧导航改为真实页面切换：总览、客户端、脚本、远程操作、设置。
- 已新增 `ClientRemoteActions.vue`，集中下发 Server 消息、更新、Service、开机启动和本机窗口命令。
- 已新增 `ScriptPanel.vue`，集中展示 Lua bootstrap、安全门和权限配置。
- 已修复 Web Admin 切换 Client 后历史趋势不立即刷新的问题。
- 已将根版本 `VERSION` 更新为 `v1.11.0`。
- 已将 `web-admin` private package 版本同步为 `1.11.0`。
- 已通过 P17 验证：Rust fmt、clippy、workspace test、Web Admin build、打包脚本、Client/Server core 包内烟测、`update.apply` 命令包内烟测和 zip 敏感文件检查。
- 已生成编译包 `WoW_Framework_v1.11.0_windows.zip`，SHA-256 为 `9027d9bd52e4b5d21aed82908280185914bd070599c0bd96ba8f2993a62b7b97`。
- 已发布 GitHub Release：`v1.11.0 - 服务端远程更新与导航补全`。
- 已上传编译包 `WoW_Framework_v1.11.0_windows.zip`，asset ID 为 `468743401`。
- 已进入 P18，固定本阶段补 Server 控制台上线日志和 Server/Client 分包输出。
- 已新增 `技术设计/服务端上线日志与分包设计.md`。
- 已新增 `计划报告/阶段P18-步骤表.md`。
- 已在 Management Server 状态上报处理里新增 `Client 上线`、`Client 状态刷新`、`Client 离线` 日志。
- 已扩展 `tools/package-release.ps1`，可输出总包、Server 分包和 Client 分包。
- 已将根版本 `VERSION` 更新为 `v1.12.0`。
- 已将 `web-admin` private package 版本同步为 `1.12.0`。
- 已通过 P18 验证：Rust fmt、clippy、workspace test、Web Admin build、打包脚本、Server 控制台上线日志烟测、分包边界检查和三类 zip 敏感文件检查。
- 已生成总包 `WoW_Framework_v1.12.0_windows.zip`，SHA-256 为 `af2825aeb58c1663d97d17ac1c34a53cf29bd939bcbb9fc3f57e4868fb2db95f`。
- 已生成 Server 分包 `WoW_Server_v1.12.0_windows.zip`，SHA-256 为 `f1db6f39930afdb4e2c27e465ce56ed381a3d8864e197a057ac91b2ac389a843`。
- 已生成 Client 分包 `WoW_Client_v1.12.0_windows.zip`，SHA-256 为 `16ddb34939c7404c5d38874db859c2785cb80eb7b70517248068a26aff09abe1`。
- 已发布 GitHub Release：`v1.12.0 - 服务端上线日志与分包`。
- 已上传三类编译包：总包 asset ID `468773259`、Server 分包 asset ID `468773355`、Client 分包 asset ID `468773378`。
- 已进入 P19，固定本阶段只修正式直启、离线状态和远程目标选择，不扩展鉴权审计和命令回执。
- 已新增 `技术设计/客户端直启与远程目标选择修正设计.md`。
- 已新增 `计划报告/阶段P19-步骤表.md`。
- 已新增 Client 离线状态快照与内部 `--report-offline` 维护命令。
- 已将托盘 PowerShell 宿主和通知宿主改为隐藏启动，减少黑色控制台窗口。
- 已将托盘停止 monitor 时补发 `online=false` 状态。
- 已将 Server 心跳兜底窗口从 60 秒收敛为 30 秒。
- 已扩展发布打包脚本，发布包 Client 配置默认启用本机 Server 上报并修正 DmBridge 包内路径。
- 已扩展 Web Admin 远程操作面板，可选择单台 Client 或全部已上报 Client 下发消息和命令。
- 已将根版本 `VERSION` 更新为 `v1.13.0`。
- 已将 `web-admin` private package 版本同步为 `1.13.0`。
- 已通过 P19 验证：Rust fmt、clippy、workspace test、Web Admin build、打包脚本、Client 分包无环境变量上报烟测、离线上报烟测、PE 子系统检查和三类 zip 敏感文件检查。
- 已生成总包 `WoW_Framework_v1.13.0_windows.zip`，SHA-256 为 `aab06de2db6a0e99d2bbeec0a40826d6808d72ac2ed35a6aa9324a0ba8fa2a58`。
- 已生成 Server 分包 `WoW_Server_v1.13.0_windows.zip`，SHA-256 为 `cdaa07379cb04ddcb11274b3d8a15adff965855f04d497ec8c180a7e5303e77e`。
- 已生成 Client 分包 `WoW_Client_v1.13.0_windows.zip`，SHA-256 为 `26cada72507ff5b051ea162d3a3cf02c2d5463abcfc0f5ca479247e817ad3bd9`。
- 已发布 GitHub Release：`v1.13.0 - 客户端直启与远程目标选择修正`。
- 已上传三类编译包：总包 asset ID `468859713`、Server 分包 asset ID `468859748`、Client 分包 asset ID `468859793`。
- 已进入 P20，固定本阶段只修复 Client 正式直启链路，不扩展命令回执、鉴权审计或安装器形态。
- 已新增 `计划报告/阶段P20-步骤表.md`。
- 已新增统一 PowerShell 脚本写入工具，生成 UTF-8 BOM 脚本以兼容 Windows PowerShell 5.1。
- 已将托盘宿主、设置窗口和通知链路切换为 Windows PowerShell `-STA` 启动。
- 已将 GUI launcher、托盘、通知和 updater 隐藏启动收敛为 `CREATE_NO_WINDOW`，不再使用 `DETACHED_PROCESS`。
- 已将 `tools/package-release.ps1` 和 `实现模块/dm-bridge/build.ps1` 转换为 UTF-8 BOM，修复中文路径打包解析问题。
- 已将根版本 `VERSION` 更新为 `v1.13.1`。
- 已将 `web-admin` private package 版本同步为 `1.13.1`。
- 已通过 P20 验证：Rust fmt、client-agent 测试、workspace 测试、clippy、Web Admin build、打包脚本和 Client 分包根目录 exe 真实直启烟测。
- 已生成总包 `WoW_Framework_v1.13.1_windows.zip`，SHA-256 为 `3ece2cafba9063ff122a5393179b8fe5cdaf8b7c25a431ff3653cfe3ccaf137a`。
- 已生成 Server 分包 `WoW_Server_v1.13.1_windows.zip`，SHA-256 为 `00b63001a781d6c19bc9ef85b47c03677a803574081d7a74939296ea0667e6f3`。
- 已生成 Client 分包 `WoW_Client_v1.13.1_windows.zip`，SHA-256 为 `85206e3bcf2079f75443794bff7685a9660159017ec0218bfeb42ad4d4dfa292`。

## 进行中
- P20 GitHub Release 发布。

## 下一步计划
1. 发布 v1.13.1 GitHub Release，上传总包、Server 分包和 Client 分包。
2. 继续禁止提交签名私钥、真实脚本、账号资料、大漠二进制和 JSONL 运行数据。
