# 风险与问题记录

| 编号 | 当前问题 | 风险描述 | 风险等级 | 影响范围 | 解决方案 | 当前状态 |
|------|----------|----------|----------|----------|----------|----------|
| R001 | GitHub 远端未确认 | 无法立即推送远端备份 | 中 | 版本备份 | 已绑定 `https://github.com/cowmaxs66/Wow_Web.git` 并推送 `main` | 已完成 |
| R002 | Windows Service 与桌面自动化存在冲突 | Service 运行在 Session 0，可能无法操作用户桌面 | 高 | Client 部署 | P1 前期采用用户态 Agent，后期再设计 Service + Session Broker | 进行中 |
| R003 | 大漠 COM 线程模型复杂 | 多线程直接调用可能导致不稳定 | 高 | DmBridge | 已实现 Bridge 内部 STA Worker 队列，并通过 Rust/Lua COM 烟测 | 已完成 |
| R004 | 远端脚本下发存在安全风险 | 未签名脚本可能导致远程执行风险 | 高 | 脚本管理 | 已完成本机 manifest、Ed25519 签名、SHA-256 和权限白名单；远程下发未纳入 v1.0.0，生产前必须单独设计鉴权、审批、审计和回滚 | 进行中 |
| R005 | 当前 GitHub 仓库为 public | 误提交密钥、真实账号或商业脚本会造成泄露 | 高 | 版本管理、脚本配置 | 禁止提交 `.env`、真实账号、真实脚本配置；公开仓库只保存框架与示例 | 进行中 |
| R006 | 大漠资料包含二进制与可能受限文档 | 误提交 CHM、DLL、注册码资料会造成版权或敏感信息问题 | 高 | 版本管理 | `.gitignore` 增加 CHM、CHW、RAR、DAT、私有资料目录；只提交接口摘要 | 进行中 |
| R007 | 大漠高风险接口过多 | 内存、汇编、防护盾、变速等接口会扩大风险面 | 高 | DmBridge、Lua API | 第一版明确排除高风险接口，只做图色、窗口、后台、键鼠闭环 | 进行中 |
| R008 | DLL 字符串内存边界不清 | 跨 DLL 分配/释放字符串会造成内存错误或乱码 | 高 | DmBridge、Rust FFI | 已实现 UTF-16 调用方 buffer 输出，并通过 `Ver/GetColor` 字符串烟测 | 已完成 |
| R009 | DmBridge 当前仍是最小直接 STA 模式 | Rust 多线程调用前如果不升级队列，会触发线程错误或 COM 不稳定 | 高 | DmBridge、Rust FFI | 已升级为内部 STA Worker 队列，导出 ABI 保持不变，并通过 Win32 编译和导出符号检查 | 已完成 |
| R010 | Delphi ABI 常量与导出函数同名 | Delphi 标识符不区分大小写，`DM_BRIDGE_ABI_VERSION` 会被解析为 `dm_bridge_abi_version`，导致递归栈溢出 | 高 | DmBridge ABI | 已将内部常量改名为 `DM_BRIDGE_CURRENT_ABI_VERSION`，并通过 32 位 P/Invoke 与 Rust ABI 测试 | 已完成 |
| R011 | 大漠无参 COM 方法调用写法错误 | `FObj.Ver()` 在 Delphi OleVariant 下会触发“无效的参数数目” | 中 | DmBridge Basic/Input | 已改为 `FObj.Ver`、`FObj.GetLastError`、`FObj.LeftClick`、`FObj.UnBindWindow` | 已完成 |
| R012 | Client 手写 HTTP 响应解析过窄 | Axum/Hyper 响应格式可能与假 Server 测试不同，导致真实上报失败 | 中 | Client/Server 通讯 | 已支持 CRLF/LF 分隔、chunked body，并移除不必要的 TCP 写端 half-close | 已完成 |
| R013 | P4 开发期 CORS 边界宽 | 当前 `CorsLayer::permissive()` 便于本地 Web Admin 联调，但生产部署不能继续放开来源 | 中 | management-server、web-admin | 已在 P6 安全边界说明中明确生产前必须收紧；功能实现未纳入 v1.0.0 | 进行中 |
| R014 | 签名私钥管理尚未产品化 | P5 只验证签名，不提供私钥保存、轮换、吊销和签名工具链 | 高 | 脚本管理、发布流程 | P6 已补发布侧密钥边界说明；签名工具链和私钥轮换未纳入 v1.0.0 | 进行中 |
| R015 | 脚本内容变更需要同步 manifest | 修改 Lua 文件后如果未更新 hash 和签名，Client 会拒绝启动 | 中 | client-agent、脚本维护 | 已在部署运行指南中明确记录，这是预期安全行为；后续可用签名工具降低人工错误 | 已完成 |
| R016 | v1.0.0 不是生产部署版本 | 当前缺少鉴权、TLS、生产 CORS、数据库持久化、审计和脚本发布后台 | 高 | 发布与部署 | P6 发布说明和安全边界已明确当前只做源码与文档归档，生产化必须另立阶段设计 | 已完成 |
| R017 | Web 分析只有当前快照 | Management Server 当前只保存每个 Client 最新状态，无法提供历史趋势和长期在线率 | 中 | web-admin、management-server | P7 明确只做快照分析；历史分析必须先引入数据库和指标模型 | 已完成 |
| R018 | 状态协议扩展存在旧二进制混用风险 | 修改 `ClientStatus` 后，如果直接运行旧 `target/debug/client-agent.exe`，Server 会收到旧字段 | 中 | client-agent、web-admin、测试验证 | P7 验证中已发现并记录，发布烟测前必须执行 `cargo build --workspace` 重建普通二进制 | 已完成 |
| R019 | P8 历史记录不持久 | 当前历史只保存在 Management Server 进程内，Server 重启后历史丢失 | 中 | management-server、web-admin | P8 已明确为短期历史；生产持久化需 P9 设计数据库、索引、保留策略和备份恢复 | 已完成 |
| R020 | 前端 SVG 样式选择器过宽 | 历史趋势面板初版 `svg` 样式影响了 lucide 小图标，导致图标被放大 | 低 | web-admin | 已将样式收敛为 `.trend-box svg/line/polyline/circle`，并通过截图复查 | 已完成 |
| R021 | JSONL 历史文件可能包含运行信息 | Client 状态历史包含脚本名、版本、上报目标和运行摘要，误提交会泄露环境信息 | 中 | management-server、版本管理 | 已忽略 `data/` 和 `*.jsonl`，普通编译包也不包含 JSONL 历史文件 | 进行中 |
| R022 | 32 位大漠不能被 x64 Client 直接加载 | `dm.dll` 按 32 位插件处理，x64 Client 直接加载会失败或不稳定 | 高 | client-agent、DmBridge、发布包 | P10 发布包同时提供 x64 Client 和 x86 Client；Web 向导与脚本明确区分 x64 核心模式和 x86 DM 模式 | 进行中 |
| R023 | 源码一键脚本可能复用旧 `target/debug` 二进制 | 版本更新后直接运行旧 exe 会输出旧 `release_version`，导致验证误判 | 中 | tools、测试验证 | P10 已改为源码目录执行时先 `cargo build`，发布包目录才直接使用 `bin/` 产物 | 已完成 |
| R024 | Windows PowerShell 中文路径兼容不稳定 | 中文目录名下 `-File` 调用可能出现解析异常，影响一键脚本体验 | 中 | tools、发布包 | P10 将正式脚本目录固定为 ASCII `tools/`，`.cmd` 优先调用 `pwsh`，无 `pwsh` 时回退 Windows PowerShell | 已完成 |
| R025 | P11 消息队列尚未持久化 | Server 重启后消息队列丢失，Client 也没有送达确认 | 中 | management-server、client-agent | P11 明确只做本机试运行消息；P12/P13 再设计持久化、确认、权限和审计 | 进行中 |
| R026 | P11 通知不是完整托盘程序 | 当前通知使用系统气泡，尚无持久托盘图标、右键菜单和原生设置窗口 | 中 | client-agent、用户体验 | P11 先交付可测 monitor/log/notify；完整托盘 UI 单独进入 P12 | 进行中 |
| R027 | 开机启动路径随发布包移动失效 | HKCU Run 记录的是当前 exe 绝对路径，用户移动目录后旧路径会失效 | 中 | client-agent、用户体验 | 用户移动目录后重新执行 `client-agent.exe --enable-startup`；后续原生设置窗口可增加路径状态检查 | 进行中 |
| R028 | Service 无法显示交互窗口 | Windows Service 运行在 Session 0，不能直接打开托盘和设置窗口 | 高 | client-agent、用户体验 | P13 将 Service 与托盘分离：Service 只跑 monitor，托盘和设置窗口在当前用户 Session 运行 | 进行中 |
| R029 | 远程命令缺少鉴权与审计 | 当前 Server 命令 API 为本机试运行白名单队列，尚未加入登录、授权、审计和确认 | 高 | management-server、web-admin、client-agent | P13 只允许固定白名单命令并消费队列；生产联网前必须补鉴权、审计、持久化和送达确认 | 进行中 |
| R030 | 自动更新不做运行中自替换 | 直接替换正在运行的 exe 可能造成损坏或半更新 | 中 | client-agent、发布包 | P16 已新增独立 updater 脚本：先退出/停止旧进程，再备份并覆盖安装目录 | 已完成 |
| R031 | 无参数入口改变旧脚本假设 | 旧脚本若依赖 `client-agent.exe` 输出 JSON，会改为启动托盘 | 中 | client-agent、tools、测试脚本 | 已新增 `--run-once` 作为兼容维护入口，文档和工具脚本已同步更新 | 已完成 |
| R032 | Server 无参数会打开浏览器 | 自动化场景可能不希望弹出浏览器 | 低 | management-server、tools | 已新增 `--no-open-browser` / `--api-only`，工具脚本在非 OpenBrowser 模式下显式传入 | 已完成 |
| R033 | GUI launcher 启动失败无控制台反馈 | 无控制台入口启动失败时用户看不到错误文本 | 中 | wow-launcher、发布包 | launcher 启动失败会写入 `logs/launcher-error.log`；包内保留 `bin/*-core.exe` 维护入口 | 已完成 |
| R034 | 当前用户安装不是系统级安装器 | 当前阶段不生成 MSI/MSIX，不处理管理员级全局安装、提权和系统服务自动安装 | 中 | 发布包、用户体验 | P15 明确只做当前用户安装、桌面/开始菜单快捷方式和卸载入口；系统级安装器后续单独设计 | 进行中 |
| R035 | Windows PowerShell 5.1 解析 UTF-8 脚本不稳定 | 无 BOM UTF-8 脚本中中文字符串会被 5.1 按 ANSI 误读，可能破坏语法 | 中 | tools、installer | 安装/卸载脚本已改为 ASCII；打包脚本建议使用 `pwsh` 执行以兼容中文源码路径 | 已完成 |
| R036 | 自动更新尚未验证发布包签名 | 当前只从 GitHub Release 下载 zip，尚未校验独立签名和证书链 | 高 | updater、发布包 | P16 只做自替换闭环；P17 前必须设计 SHA/digest 校验、签名验证、回滚和审计 | 进行中 |
| R037 | 更新过程中正在运行的业务脚本会被停止 | updater 会停止安装目录下旧进程，可能中断当前自动化任务 | 中 | client-agent、用户体验 | 托盘入口命名为“安装更新并退出”，更新前会停止 monitor；后续可增加确认窗口和任务空闲判断 | 进行中 |
| R038 | 远程更新命令缺少执行回执 | Server 只能确认命令写入队列，不能确认 Client 是否已完成更新 | 中 | management-server、web-admin、client-agent | P17 在 Web 中明确显示“写入命令队列”，执行结果以 Client 本机日志为准；后续需补命令结果回传和审计记录 | 进行中 |
| R039 | 在线状态阈值当前固定 | Server 查询层使用固定心跳窗口，生产环境不同频率可能误判离线 | 中 | management-server、web-admin | P19 已将兜底窗口收敛为 30 秒；后续将阈值改为配置项并显示最后上报年龄 | 进行中 |
| R040 | GUI launcher 不显示 Server 控制台日志 | 根目录 `management-server.exe` 为无控制台入口，用户双击后看不到 Client 上线日志 | 中 | management-server、发布包 | P18 明确需要查看控制台时运行 `bin/management-server-core.exe --no-open-browser`，并在部署指南和分包 README 中说明 | 进行中 |
| R041 | 分包可能被误用为自动更新包 | 如果 updater 下载到 Server 分包或缺少 Client 文件的分包，会造成半更新 | 高 | client-agent、发布包 | P18 保留 `WoW_Framework` 总包作为 updater 匹配目标，Server/Client 分包命名为 `WoW_Server` 和 `WoW_Client`，避免匹配当前 updater 规则 | 进行中 |
| R042 | 非正常杀进程仍可能短暂显示在线 | 托盘正常退出会主动上报离线，但任务管理器强杀、断电或网络中断无法保证立即回写 | 中 | management-server、client-agent | P19 增加主动 `online=false` 上报，并把 Server 查询兜底窗口收敛为 30 秒；后续可改为可配置心跳和失联告警 | 进行中 |
| R043 | Web 批量下发暂无执行回执 | 选择全部 Client 时 Server 只能确认写入各自队列，不能确认每台实际执行完成 | 中 | web-admin、management-server、client-agent | P19 只补明确目标选择；执行结果仍以客户端本机日志为准，后续单独设计命令回执和审计 | 进行中 |
| R044 | Client 分包根目录 exe 双击无效果 | Windows PowerShell 5.1 会把 UTF-8 无 BOM 托盘脚本中的中文字符串按 ANSI 误读，加上非 STA/DETACHED 隐藏启动后表现为无进程、无日志、无反馈 | 高 | client-agent、wow-launcher、发布包 | P20 改为 UTF-8 BOM 写入脚本、PowerShell `-STA` 启动、隐藏启动只保留 `CREATE_NO_WINDOW`，并通过 Client 分包根目录 `client-agent.exe` 真实直启烟测 | 已完成 |
| R045 | 中文源码路径下发布脚本可能乱码 | Windows PowerShell 5.1 执行 UTF-8 无 BOM 的打包/构建脚本时会把 `实现模块` 等路径解析为乱码，导致发布失败 | 中 | tools、dm-bridge、发布流程 | P20 将 `tools/package-release.ps1` 和 `实现模块/dm-bridge/build.ps1` 转换为 UTF-8 BOM，并重新完成 v1.13.1 打包 | 已完成 |
| R046 | Server 托盘不是 Windows Service | P21 的 Server 托盘只能控制当前用户启动的 Server 进程，不能提供系统服务自恢复、开机后台运行和服务恢复策略 | 中 | management-server、发布包、用户体验 | P21 明确只做用户态托盘快捷操作；Server Windows Service、恢复策略和安装器服务化后续单独设计 | 进行中 |
| R047 | 托盘关闭 Server 会中断所有 Client 上报 | 用户通过 Server 托盘关闭或重启 Server 时，在线 Client 会暂时无法上报和拉取命令 | 中 | management-server、client-agent | 托盘菜单使用明确文案“关闭/重启 Server”；Client 仍按 monitor 周期重试，后续可补 Server 操作确认和告警 | 进行中 |
| R048 | 控制中心仍不是系统安装器 | 当前控制中心只做当前用户目录安装，不提供 MSI/MSIX、签名证书、系统级卸载登记和企业部署能力 | 中 | 安装体验、发布包 | P22 明确不进入系统安装器；后续如要商业分发，需要单独设计 MSI/MSIX、代码签名和升级回滚策略 | 进行中 |
| R049 | 控制中心按钮操作缺少进度条 | 安装/修复和卸载当前只等待脚本完成并显示结果，耗时操作期间缺少细粒度进度反馈 | 低 | 用户体验 | P22 先完成统一入口；后续可增加后台任务进度、禁用按钮和操作日志面板 | 进行中 |
| R050 | Server 不下发任意 Lua 文本 | P23 只允许 `script.run_bootstrap` 重跑 Client 本机脚本，尚未提供 Server 端脚本分发、签名、审批和回执 | 中 | 脚本管理、安全审计 | 维持安全边界；后续如做脚本分发，必须先设计签名、版本、审批、回滚和执行回执 | 进行中 |
| R051 | DM 真机链路仍依赖本地环境 | P23 Web 已说明 DM/Lua 流程，但未提交大漠二进制，也未在真实游戏窗口执行点击/绑定自动化 | 中 | DM 模式、脚本验证 | 继续保留大漠二进制和授权文件在本机私有目录；真实 DM 场景需单独手动验证 | 进行中 |
| R052 | 命令回执仍为内存审计 | P24 回执可解决近期执行结果可见，但 Server 重启后回执丢失，不能作为长期审计凭证 | 中 | management-server、web-admin | P24 只保存每台 Client 最近 100 条；后续需接入数据库、保留策略和审计导出 | 进行中 |
| R053 | 命令回执缺少操作者身份 | 当前命令 API 仍未登录鉴权，回执只能说明 Client 执行结果，不能证明谁下发了命令 | 高 | management-server、安全审计 | 生产联网前必须补登录、角色权限、操作员 ID、审批和防重放 | 进行中 |
| R054 | CI 不覆盖 Delphi 打包链路 | GitHub runner 没有 RAD Studio 与本机大漠私有环境，无法验证 DmBridge 编译和正式打包脚本 | 中 | CI/CD、发布流程 | P25 CI 只跑 Rust/Web 基础验证；发布前仍必须在本机执行 `tools/package-release.ps1` 和分包烟测 | 进行中 |
| R055 | 命令清单还不是完整 registry | P25 只把命令类型收敛到共享清单，Client 执行仍是 match 分支，新增命令仍需注册执行逻辑 | 中 | client-agent、扩展性 | 后续阶段再引入 `RemoteCommand` trait 和 registry；当前先用共享清单避免 Server/Client 白名单漂移 | 进行中 |
| R056 | 远程配置下发缺少登录鉴权 | `config.apply` 已限制白名单字段，但 Server API 仍未验证操作者身份 | 高 | management-server、web-admin、client-agent | P26 只做本机试运行闭环；生产联网前必须补登录、角色权限、操作者审计、防重放和持久化记录 | 进行中 |
| R057 | 配置写错会影响下一轮 monitor | 远程配置写回 TOML 后，如果路径或安全门参数不正确，下一轮脚本执行可能失败 | 中 | client-agent、用户体验 | monitor 重载失败时保留上一轮有效配置；配置结构保存前会完整校验，执行错误写入本机日志和命令回执 | 进行中 |
| R058 | DM smoke 依赖本机大漠 COM 环境 | 发布包包含 DmBridge，但不包含 `dm.dll`、`RegDll.dll`、授权文件；如果本机 COM 未注册，DM smoke 会失败 | 中 | client-agent、DM 模式验证 | P28 新增 `dm_smoke.lua` 只做低风险实机验证，并在文档中明确前置条件；真实大漠环境仍由用户本机私有配置提供 | 进行中 |
| R059 | 多机器轮询会放大请求量 | 旧链路每轮可能包含状态上报、消息拉取和命令拉取 3 次 HTTP 请求，Client 数量增加后请求量线性上升 | 中 | management-server、client-agent、web-admin | P30 已新增 monitor jitter 和 `/api/client/sync` 合并同步接口；后续仍需分页、分组过滤、耗时统计和持久化审计 | 进行中 |
| R060 | 批量操作误触风险 | 多机器场景下误选“全部客户端”可能把更新、停服务或配置下发到所有机器 | 高 | web-admin、management-server、client-agent | P29 已在 Web 批量目标下增加显式确认；生产联网前仍需登录鉴权、角色权限、审批和操作者审计 | 进行中 |
| R061 | 审计 JSONL 可能包含运行摘要 | 审计事件会记录 Client ID、命令类型、消息标题和回执摘要，误提交会泄露环境信息 | 中 | management-server、版本管理 | P31 审计默认不启用文件；已忽略 `*.jsonl` 和 `data/`，文档明确 JSONL 不得提交 GitHub | 进行中 |
| R062 | 审计缺少操作者身份 | 当前 Server 未接入登录鉴权，审计只能记录操作对象和结果，不能证明谁下发命令 | 高 | management-server、安全审计 | P31 只做操作事件记录；生产联网前必须补登录、角色权限、operator_id、审批和防重放 | 进行中 |
| R063 | 分页查询仍基于内存快照 | `/api/client/status-page` 查询的是 Server 当前最新状态，不是数据库索引，Server 重启后依赖历史 JSONL 回放恢复 | 中 | management-server、web-admin | P31 保持最小兼容实现；后续多机器容量测试后再决定是否引入数据库、索引和查询统计 | 进行中 |
| R064 | Release 包携带 DM 运行 DLL | `dm.dll` 和 `RegDll.dll` 进入公开 Release 后可能涉及授权范围和再分发边界 | 高 | 发布包、GitHub Release、用户分发 | P32 只把 DLL 放入 ignored release 包，不提交源码；使用前必须确认大漠授权和分发许可，后续如需公开商用必须改为私有 Release 或用户本机安装 | 进行中 |
| R065 | 只携带 DLL 不等于完成 COM 注册 | DmBridge 仍通过 `dm.dmsoft` COM 创建对象，目标机器如果未注册或未授权，大漠调用会失败 | 中 | Client 部署、DM 模式 | P32 先随包携带 `dm.dll/RegDll.dll`；测试时需要确认目标机器 COM 注册与授权状态，后续可补一键注册/检测向导 | 进行中 |
| R066 | 内部测试模式默认关闭脚本安全校验 | v1.25.0 为了快速实机测试，默认不校验 manifest、hash 和签名，脚本内容以管理端推送为准 | 中 | client-agent、web-admin、脚本测试 | 这是当前内部测试目标；Web 只允许写入 `scripts/` 目录，后续可在后台重新开启 `script_security.enabled` 并推送 manifest | 进行中 |
| R067 | Lua 停止是软停止 | `script.stop` 会关闭下一轮 Lua 执行，但不会强杀已经进入执行中的 Lua 任务 | 中 | client-agent、脚本生命周期 | 当前 Lua 执行仍依赖脚本返回和指令上限；如需强制中断，需要后续把 Lua 运行升级为可取消任务模型 | 进行中 |
| R068 | 大漠 `dx` 后台绑定可能触发 COM 内部异常 | 部分窗口、显卡、权限或桌面环境下，`BindWindow(hwnd, "dx", ...)` 可能由大漠内部抛出 Access violation | 高 | DmBridge、Lua 脚本、DM 实机测试 | P34 增加 hwnd 校验和详细错误上下文；脚本优先使用 `dm.safe_bind_window`，先测试 `normal/gdi`，再尝试 `dx` | 进行中 |
| R069 | Edge App 模式依赖本机 Edge | Server 桌面控制台优先使用 Edge `--app`，目标机器缺少 Edge 时会退回浏览器打开 | 低 | management-server、用户体验 | 保留“浏览器打开 Web 管理页”备用入口；后续如需完全内嵌可单独引入 WebView2 runtime 和安装检测 | 进行中 |
| R070 | DM 自动初始化不等于 COM 注册成功 | Lua 首次调用会自动 init DmBridge，但目标机器未注册或未授权 `dm.dmsoft` 时仍会失败 | 中 | client-agent、DM 实机测试 | P35 只消除脚本必须手写 `dm.init` 的使用门槛；后续补 DM 注册/授权检测向导 | 进行中 |
| R071 | UI 截图自动化验证受当前工具限制 | 当前环境没有项目内 Playwright 依赖，也未暴露可截图浏览器工具，P35 UI 主要通过 TypeScript 构建验证 | 低 | web-admin、测试验证 | 不引入额外测试依赖污染项目；发布后由用户实机检查桌面控制台缩放与布局 | 进行中 |
| R072 | 命令回执不适合承载大量脚本日志 | Lua `log()` 进入回执后，如果脚本高频输出，会增加 Server 内存回执体积和 Web 展示负担 | 中 | client-agent、management-server、web-admin | P36 对回执日志条数和单条长度做截断；长日志仍通过 Client 本地日志窗口查看 | 进行中 |
